# WPStudBook 設計決策

## 1. 文件資訊

- 專案：`WPStudBook`
- 狀態：階段 0 設計決策第 1 版。2026-09-14 經使用者逐段確認。
- 依據：[需求規格](需求規格.md)、[開發計畫](開發計畫.md) 第 5 章 D-01～D-12。
- 效力：本文件只決定「怎麼做」，不新增或改寫需求。與需求規格衝突時以需求規格為準，並回報衝突。
- 引用慣例：括號內的章節編號（例如「12.2」）指需求規格；「計畫第 N 章」指開發計畫；「本文件 N 節」指本文件。
- 更新規則：決策變更時同步更新本文件與計畫第 5 章，並在變更的節內註明日期與原因。

## 2. 決策總覽

| 編號 | 決策 | 內容 | 本文件 |
| --- | --- | --- | --- |
| D-01 | 程式語言與型別檢查 | TypeScript 6.0，嚴格模式 | 3 節 |
| D-02 | 套件管理、建置與單一 HTML | npm；Vite 8 ＋ vite-plugin-singlefile，輸出 `app/dist/WPStudBook.html` | 3 節 |
| D-03 | 單元測試 | Vitest，測試標題帶情境代號 | 7 節 |
| D-04 | IndexedDB 測試 | Node 以 fake-indexeddb 做整合測試；真實瀏覽器由端到端測試驗證 | 7 節 |
| D-05 | 端到端測試 | Playwright 驅動已安裝的瀏覽器，以 `file://` 開啟建置檔；本機 Edge，GitHub Actions 的 Windows 環境跑 Edge 與 Chrome | 7 節 |
| D-06 | 介面實作 | React 19 ＋ react-aria-components ＋ @tanstack/react-virtual | 3 節 |
| D-07 | 目錄結構與模組邊界 | 程式專案集中在 `app/`；`domain`、`import`、`storage`、`services`、`ui` 五層，ESLint 強制引用規則 | 4 節 |
| D-08 | 資料契約第 1 版 | 主鍵 `[gameId, id]`、目前狀態＋事件歷程、唯一索引、備份與檢查點共用格式 | 5 節 |
| D-09 | 領域用語對照 | 一個規格名詞對應一個程式名稱 | 6 節 |
| D-10 | 合成樣本 | TypeScript 產生器＋iconv-lite 輸出 CP932，產物不提交；另有本機實檔測試 | 7 節 |
| D-11 | 版本號、分支與發布 | `0.x.y` 開發、`1.0.0` 為第一版；主題分支＋PR；GitHub Release 附檔 | 8 節 |
| D-12 | 持續整合 | GitHub Actions：Ubuntu 檢查與建置，Windows 端到端，標籤觸發發布 | 8 節 |

使用者前提：程式碼幾乎都由 AI agent 閱讀與修改，因此優先選擇 agent 最熟悉、最不容易寫錯的工具與寫法。

## 3. 工具與版本（D-01、D-02、D-06）

| 用途 | 工具 | 2026-09-14 查到的版本 | 說明 |
| --- | --- | --- | --- |
| 執行環境 | Node.js、npm | Node 24、npm 11 | 本機已安裝；不另外使用其他套件管理工具 |
| 語言 | TypeScript | 6.0.3 | 見下方設定。不使用 7.0：typescript-eslint 8.70 只支援 `>=4.8.4 <6.1.0`，支援 7.0 後再評估升級 |
| 建置 | Vite ＋ @vitejs/plugin-react ＋ vite-plugin-singlefile | 8.3.0、6.1.1、2.3.3 | JS 與 CSS 全部內嵌為單一 HTML |
| 介面 | React、react-dom | 19.3.0 | — |
| 無障礙元件 | react-aria-components | 1.21.1 | 對話框、頁籤、焦點鎖定、鍵盤操作（13.4、13.5） |
| 長清單 | @tanstack/react-virtual | 3.14.13 | 清單虛擬化（12.5） |
| IndexedDB | idb | 8.0.3 | Promise 包裝，不使用 ORM |
| 單元與整合測試 | Vitest、fake-indexeddb | 5.0.0、6.2.5 | — |
| 端到端測試 | @playwright/test | 1.63.0 | 使用已安裝的 Edge、Chrome，不下載瀏覽器 |
| 程式檢查與格式 | ESLint、typescript-eslint、Prettier | 10.10.0、8.70.0、3.9.6 | 含型別感知檢查，例如沒有處理的 Promise |
| React Hooks 規則 | eslint-plugin-react-hooks | 7.1.1 | React Hooks 檢查規則（`rules-of-hooks`、`exhaustive-deps`） |
| 合成樣本 | iconv-lite | 0.7.3 | 只在開發時把 TSV 轉成 CP932 |

- 表列版本是決策當日的最新版。階段 0 安裝後以 `package-lock.json` 鎖定實際版本，並把 `package.json` 的 `engines.node` 設為 24。
- TypeScript 設定：`strict`、`exactOptionalPropertyTypes`（區分「欄位不存在」與「值為 `undefined`」，落實 5.3 未知值不是零值）、`noUncheckedIndexedAccess`（依位置讀取匯入欄位時，強制處理欄位不存在的情況）。
- 2026-09-15 補充：`iconv-lite`、`@tanstack/react-virtual` 階段 0 刻意不安裝，等實際用到時才安裝（合成樣本在階段 4；長清單於清單第一次變長時）。

## 4. 目錄與分層（D-07）

> 2026-09-15 變更：程式專案移入 `app/`，應用服務層由 `app` 改名為 `services`。原因：第 1 版把程式專案放在 repo 根目錄，與文件、專案規則混在一起；若外層改名 `app/` 而服務層仍叫 `app`，會出現意思重複的 `app/src/app`。

### 4.1 Repository 版面

```text
WPStudBook/                     repo 根目錄：只放專案規則、文件與參考資料
  AGENTS.md、AGENTS_cht.md、CLAUDE.md、.gitignore
  .github/                      GitHub Actions（GitHub 規定必須放在根目錄）
  docs/                         需求規格、開發計畫、驗收追蹤、設計決策、實作計畫
  .references/                  使用者提供的實檔（不進版本控制）
  .claude-tmp/、.codex-tmp/     Agent 暫存區（不進版本控制）
  app/                          程式專案：所有程式、設定、依賴與建置測試產出
    package.json、package-lock.json、tsconfig.base.json、tsconfig.json、WPStudBook.html
    vite.config.ts、vitest.config.ts、playwright.config.ts
    eslint.config.js、eslint.layers.js、.prettierrc.json、.prettierignore
    src/  tests/  scripts/
    node_modules/  dist/  test-results/  playwright-report/   （不進版本控制）
```

- 規則寫在專案 `AGENTS.md` 的 Repository Layout，所有 agent 都必須遵守。
- npm、建置、程式檢查與測試指令一律在 `app/` 內執行；不得在根目錄執行 `npm install` 或建立工具設定。
- 新增根目錄檔案或資料夾前，必須取得使用者同意。
- 資料夾名稱不得重複上層的意思，例如 `source/src`、`app/src/app`。
- 程式從 `app/` 讀取 repo 根目錄的資料時使用相對路徑，例如 `../docs/需求規格.md`、`../.references/`。
- 2026-09-15 補充：TypeScript 設定分成 `tsconfig.base.json`（共用編譯選項）、`tsconfig.json`（測試、腳本與設定檔，含 Node 型別）與 `src/tsconfig.json`（瀏覽器程式，不含 Node 型別）。原因：階段 0 單一設定讓瀏覽器程式也看得到 Node 型別，誤用 Node API 時型別檢查不會發現。

### 4.2 程式專案（`app/`）與分層

```text
app/
  src/
    domain/    純函式規則：身分判斷、系與代數、建系分支與循環任務、血統檢查、匯入責任與差異
    import/    解碼、分隔、依位置取值、各格式解析器 → 產生預覽資料，不寫資料庫
    storage/   IndexedDB 結構與交易、備份還原、檢查點
    services/  應用服務：每個完整操作（配種、匯入套用、回溯…）組合 domain、import、storage
    ui/        React 畫面與元件
    main.tsx
  tests/
    unit/         domain 與 import 解析、services 個別函式、scripts 工具腳本測試（Node）
    integration/  services ＋ storage（Node ＋ fake-indexeddb）
    e2e/          Playwright，只測建置後的 app/dist/WPStudBook.html
    references/   本機實檔測試（見本文件 7.4 節）
    fixtures/     合成樣本產生器的設定
  scripts/        合成樣本產生器、情境覆蓋清單等工具
```

> 2026-09-15 補充：`tests/unit/` 也包含 `services` 個別函式（例如 `environment-check.ts`）與 `scripts/` 工具腳本自身的測試，不只 domain 與 import 解析。

引用規則以 ESLint（`no-restricted-imports`、`no-restricted-globals`）強制，違反時檢查失敗：

| 層 | 可以引用 | 不可引用 |
| --- | --- | --- |
| `domain` | 只用標準 JavaScript | 其他各層、React、`idb`、瀏覽器全域物件（`window`、`document`、`indexedDB`、`localStorage` 等） |
| `import` | `domain` | `storage`、`services`、`ui`、React、`idb` |
| `storage` | `domain`（只限型別） | `import`、`services`、`ui`、React |
| `services` | `domain`、`import`、`storage` | `ui`、React |
| `ui` | `services`；`domain`（只限型別） | `storage`、`import`、`idb` |

- 2026-09-15 補充：`src/main.tsx` 只能引用 `ui`；`src` 全部禁止動態 `import()`，並禁止對外連線、裝置存取與直接存取檔案系統的 API（`fetch`、`WebSocket`、`EventSource`、`navigator.sendBeacon`、`showSaveFilePicker` 等），作為 BLD-04「不附加遊戲程序、不讀記憶體、不控制 CE、不寫回遊戲」的程式檢查。原因：階段 0 的規則沒有涵蓋 `main.tsx` 與動態 import，BLD-04 也需要可自動執行的證據。

## 5. 資料契約第 1 版（D-08）

### 5.1 總原則

1. **主鍵 `[gameId, id]`**：各局資料表的主鍵都是 `[gameId, id]`，`id` 只需在同一局內唯一（`crypto.randomUUID()`）。還原為新遊戲局時只換 `gameId`，互相引用的 `id` 不必重編；刪除整局只刪一個主鍵範圍。
2. **目前狀態＋事件歷程**：紀錄保存目前狀態供查詢，每次變更在同一個交易內寫入 `events`，歷程只新增不改寫（5.3）。寫入遊戲局資料時，遊戲局的更新時間與程式版本在寫入交易內讀出遊戲局後合併更新，不以操作開始時讀到的舊紀錄覆蓋。（2026-09-15 補充，階段 2-1。原因：以操作開始時讀到的整筆紀錄寫回，會把期間其他遊戲局層級的寫入靜默還原，例如最近備份或其他分頁更新的目前遊戲年。）
3. **唯一規則做成唯一索引**：規格要求唯一的組合，建立 IndexedDB 唯一索引作為第二道防線。IndexedDB 不會為缺少索引欄位的紀錄建索引，因此未填能力番号的手動紀錄不受「能力番号＋出生年」唯一索引限制，符合 6.2。
4. **可推導的資料不存**：任務、年齡、母馬群數量、追蹤名、輪空、場地型摘要都由現有資料推導（5.3）。規則快照例外，必須保存（7.4）。

### 5.2 資料表

| 資料表 | 主鍵 | 內容 | 索引（★ 唯一） |
| --- | --- | --- | --- |
| `games` | `id` | 名稱、程式版本（最後寫入此局的 `appVersion`）、起始年、目前遊戲年、建立與更新時間、最近備份摘要 | — |
| `gameSettings` | `gameId` | 定年（25）、高齡提醒（18）、種牡馬提醒年齡（26）、活力建議門檻、檢查點保留數（15）、顯示設定 | — |
| `lines` | `[gameId, id]` | 系位置、目前子系統與親系統、代表色、建系分支（開啟時的產出代數與年份）、已成立的母馬世代 | ★ `[gameId, position]` |
| `systemMap` | `[gameId, id]` | 子系統→親系統 | ★ `[gameId, subsystem]` |
| `horses` | `[gameId, id]` | 能力番号、出生年、性別、完整與基本馬名、正式馬名、父母（內部 `id` 或外部名稱）、自身父系、牝系；階段馬番号 `stageNumbers[]` 與名稱別名 `aliases[]` 內嵌；自家產駒的去向 `fate` | ★ `[gameId, abilityNo, birthYear]`；`[gameId, sireId]`；`[gameId, damId]`；`nameKeys`（多值，見下方說明） |
| `mares` | `[gameId, id]`（與馬匹相同） | 母馬群、來源、在圈狀態與離圈原因、據點、姊妹接替狀態、今年計畫 | `[gameId, status]`；`[gameId, group.position, group.generation]` |
| `stallionDuties` | `[gameId, id]` | 系位置、代數、馬匹、角色、任期狀態、預定後繼就緒狀態、起訖年、更換原因 | `[gameId, position, generation]`；`[gameId, horseId]` |
| `mareYearly` | `[gameId, id]` | 5 月與 7 月活力、仔出、繁殖年數與頭數 | ★ `[gameId, horseId, gameYear]` |
| `stallionYearly` | `[gameId, id]` | 能力、`サ`、仔出、種付料、戦績；與前一份相同時不寫 | ★ `[gameId, horseId, gameYear]` |
| `breedings` | `[gameId, id]` | 配種類型、實際種牡馬（內部 `id` 或外部名稱）、受胎狀態、預定生產年、產駒、規則快照、血統檢查與確認、是否偏離規則 | ★ `[gameId, mareId, gameYear]`；`[gameId, stallionId]` |
| `matingRatings` | `[gameId, id]` | 總合評價、爆發力 | ★ `[gameId, stallionId, mareId, gameYear]` |
| `foals` | `[gameId, id]`（與馬匹相同） | 母馬、出生年、能力與適性、距離適性、系與代數、牧場處置、是否自由配種、備註 | ★ `[gameId, damId, birthYear]`；`[gameId, lineage.position, lineage.generation]` |
| `recoveries` | `[gameId, id]` | 斷血補系的系、原因、年度、中斷側、補入親馬、進行狀態 | `[gameId, status]` |
| `imports` | `[gameId, id]` | 類型、年、時點、檔名、SHA-256、結果摘要 | `[gameId, type, gameYear, timing.month, timing.week]` |
| `events` | `[gameId, id]` | 對象、事件類型、遊戲年與時點、前後值、來源、現實時間 | `[gameId, subjectId]`；`[gameId, type, gameYear]` |
| `checkpoints` | `[gameId, id]` | 年、時點、建立時間、註記、釘選、SHA-256、大小、各資料表筆數 | `[gameId, gameYear]` |
| `checkpointData` | `[gameId, id]`（與檢查點相同） | 該局的 gzip 備份內容（本文件 5.5 節） | — |
| `archives` | `id` | 封存索引：名稱、版本、年份範圍、時間、檔名、筆數、驗證摘要 | — |
| `appMeta` | `key` | 應用層狀態，例如目前開啟的局 | — |

- **名稱查詢**：IndexedDB 的多值索引不能和複合鍵合用，所以 `horses.nameKeys` 存成「`gameId` ＋ 分隔字元 ＋ 名稱」的字串陣列，包含完整馬名、基本馬名、正式馬名與別名，供匯入時的精確名稱對照（11.8 父馬名稱對照等）。`nameKeys` 不收錄於備份（本文件 5.4 節）。
- **關鍵字搜尋**：部分比對無法使用索引，在記憶體中比對已載入的名稱與別名（13.3、12.5）。
- 2026-09-15 補充：`games` 保存 `appVersion`，對應需求規格 12.1「版本」。整局層級事件的 `subjectId` 固定為 `game`，不使用 `gameId`，還原為新遊戲局時不必改寫；事件型別目前有 `gameYearChanged`（目前遊戲年變更）與 `schemaMigrated`（結構遷移），各階段新增時補列。還原與回溯本身不寫事件，讓還原後的集合筆數與來源一致；經過遷移時寫一筆 `schemaMigrated`。
- 2026-09-15 補充：`checkpoints` 保存各資料表筆數 `counts`，回溯預覽不必先解壓縮就能顯示將捨棄的資料（CKPT-04）。「較晚的檢查點」指建立時間晚於回溯目標者；保留數超過時依建立時間由舊到新刪除未釘選者，剛建立的檢查點不在清除候選內；其餘都已釘選時總數可以超過保留數（原因：否則釘選數達到保留數時，新檢查點會在建立的同一交易中被刪除）。
- 2026-09-15 補充（階段 2-1）：`lines` 保存 `branch: { targetGeneration, openedYear }`（開啟分支時的產出代數與年份，第 1 系起點為 1）與 `establishedGenerations: [{ generation, gameYear }]`（已成立的母馬世代，成立後不可回溯，需求規格 8.2）。`horses.sex` 必填，`stageNumbers` 與 `aliases` 一律存陣列，沒有資料時為空陣列。`nameKeys` 的每個值為「`gameId` ＋ U+001F ＋ 名稱」，名稱取完整馬名、基本馬名、正式馬名與別名，去除前後空白後捨棄空字串並去除重複；精確名稱查詢同樣去除前後空白（需求規格 11.3）；寫入馬匹、還原與回溯時由 storage 重建。事件型別新增 `systemMapChanged`（系統對照表新增、修改或刪除，`subjectId` 為對照表紀錄 id）、`horseCreated`（手動建立馬匹）、`lineOpened`（開啟系位置，`subjectId` 為系位置 id）、`stallionDutyStarted`（種牡馬開始任期，`subjectId` 為馬匹 id）。原因：階段 2 開始寫入這些資料表，備份驗證需要明確的欄位規則。
- 2026-09-15 補充（階段 2-2）：`mares` 保存 `group`（`{ kind: 'own' | 'substitute' | 'starter', position, generation }` 或 `{ kind: 'unassigned' }`；起點母馬群固定為第 1 系、代數 0，替代與自家母馬群代數 1 以上）、`origin`、選填的 `originNote`（來源備註）、`status`、離圈時必填的 `leftReason`、`site`、選填的 `succession`（自家母駒轉入後才有）與選填的 `yearPlan: { plan, gameYear }`。母馬本身的代數由母馬群推導：自家母馬為所屬世代，市場母馬（起點用、替代）為零代，不另存。`mareYearly` 保存 `horseId`、`gameYear` 與選填的 `vitalityMay`、`vitalityJuly`、`kodashi`（0～15）、`breedingYears`、`breedingCount`（0～99）。事件型別新增 `mareAdded`、`mareSold`、`mareTransferred`（含 `timing`）、`mareYearlyChanged`（保存前後值），`subjectId` 皆為馬匹 id；`settingsChanged` 的 `subjectId` 為 `game`。今年計畫不寫事件。基本馬名去除 `(外)`、`[地]` 前綴時一併去除前綴前後的空白，沒有前綴的馬名保持原文。依使用者裁定：卡片與篩選的活力只看目前遊戲年（7 月確認值優先，其次 5 月，否則待更新；已離圈為不適用）；今年計畫的年份不等於目前遊戲年時視為待定；有活力建議門檻時，達到門檻者（含増強中）排在前面；手動轉場必填月與週。其他規則：市場母馬的預設來源，起點用或替代尚未開啟的系為市場創系、替代已開啟的系為市場補血，使用者可以改；新增起點或替代母馬不要求該系已開啟；高齡提醒年齡為 1～99，活力建議門檻為 0～100 或空白（不使用）；牝系名稱與「不屬於具名牝系」擇一，都沒填時為未取得；只能賣出生產中的母馬，有姊妹接替狀態時一併改為已售出。原因：子計畫 2-2 開始寫入繁殖牝馬與年度資料。

### 5.3 值的表示

| 情況 | 表示方式 | 例子 |
| --- | --- | --- |
| 未取得、未知 | 欄位不存在。不用 `null`、`-1` 等特殊值，不存 `undefined` | 手動新增時沒填的仔出、父馬 |
| 空白本身有意義 | 只有規格明寫空白意義的欄位存空字串 `""` | 牝系空白＝不屬於具名牝系（8.8） |
| 多種狀態 | 明確列舉每種狀態 | 活力 `{ state: 'notApplicable' }`、`{ state: 'pending' }`、`{ state: 'confirmed', value, boosted }`；受胎狀態欄位不存在＝未登記，否則存日文原文 `空胎`、`受胎`、`不受胎`、`未確認` |
| 番号 | 整數 0～65535；顯示為 `0x` 開頭的 4 位大寫十六進位 | `0x0000` 存為 `0`，是有效值 |
| 布林 | 只用於真正只有是否的事實，不代表未知 | `boosted`、`freeBred`、`pinned` |
| 遊戲時間 | 遊戲年、月、週各為整數 | `gameYear: 1968`、`timing: { month: 5, week: 1 }` |
| 現實時間 | ISO 8601 UTC 字串 | `2026-09-14T12:00:00Z` |
| 系統名稱 | 去掉結尾「系」 | `エクリプス系` → `エクリプス` |
| 馬名 | 保留原文，不做全面修剪（11.1） | — |

### 5.4 備份格式

```json
{
  "format": "WPStudBook-backup",
  "schemaVersion": 1,
  "appVersion": "0.1.0",
  "exportedAt": "2026-09-14T12:00:00Z",
  "game": { "name": "…", "startYear": 1968, "currentYear": 1969 },
  "counts": { "horses": 1234, "mares": 88 },
  "sha256": "…",
  "collections": { "gameSettings": [ … ], "horses": [ … ], "mares": [ … ] }
}
```

- **收錄範圍**：`collections` 收錄該局除了 `checkpoints`、`checkpointData` 以外的所有資料表。檢查點是本機回溯點，本身就是備份，不重複收錄。`archives` 與 `appMeta` 屬於應用層，不收錄。`games` 以 `game` 摘要（名稱、起始年、目前遊戲年）表示，不放在 `collections`；`gameSettings` 在備份中是只有一筆、不含 `gameId` 的陣列；`counts` 列出 `collections` 的每個資料表，0 筆也列出。
- **不存 `gameId`**：備份內的紀錄都不帶 `gameId`，匯入時才加上目標局的 id，因此還原為新遊戲局不必改寫資料，也不會混入其他局。含 `gameId` 或可由其他欄位重建的推導欄位也不收錄：`horses.nameKeys` 匯出時剔除、匯入時忽略，由寫入馬匹的程式依名稱欄位重建（階段 2 實作）。（2026-09-15 補充：`nameKeys` 含 `gameId`，原設計還原為新遊戲局後會指向舊局。）
- **SHA-256**：以 `{ "collections": …, "game": … }` 的標準化 JSON 計算。規則是欄位名依字典序排序、每個資料表的紀錄依 `id` 排序、陣列保持原順序、不含空白、UTF-8 編碼。同樣的資料永遠得到同樣的雜湊。（2026-09-15 變更：雜湊範圍加入 `game` 摘要。原因：原本只算 `collections`，局名、起始年與目前遊戲年被修改時無法發現。）
- **壓縮**：支援 `CompressionStream` 時輸出 `.json.gz`，否則輸出 `.json`（12.2、DATA-06）。匯入時以檔頭 `1F 8B` 判斷是否為 gzip，不看副檔名。
- **匯入順序**：解析 → 格式與版本 → 筆數 → 雜湊 → 遷移 → 欄位 → 識別與關聯 → 預覽 → 單一交易寫入。任一步失敗時資料不變（12.2、DATA-04）。（2026-09-15 變更：原順序為「解析 → 格式與版本 → 欄位 → 關聯 → 筆數 → 雜湊」。原因：筆數與雜湊必須以檔案原版本的內容驗證，欄位與關聯規則以目前版本定義，所以遷移放在雜湊之後、欄位之前。）
- **欄位規則**：`horses`、`lines`、`systemMap`、`stallionDuties`、`mares`、`mareYearly`、`events` 依 `app/src/storage/backup/record-rules.ts` 檢查必填欄位、型別、列舉值與範圍；其他資料表在定義型別的階段補上規則，之前只檢查通用規則（物件、非空 `id`、不含 `gameId`、只含 JSON 值）。規則不檢查未列出的欄位。（2026-09-15 補充，階段 2-1。`mares`、`mareYearly` 於階段 2-2 加入。）
- **原始資料匯出**：本機資料不符合資料契約、無法產生備份時，資料管理頁提供「匯出原始資料」：不驗證、不壓縮，`format` 為 `WPStudBook-raw-export`、`restorable` 為 `false`，內容是遊戲局紀錄與全部備份資料表的原樣快照，檔名含「原始資料_不能還原」；只供保存與除錯，匯入時以格式不符拒絕，也不記錄最近備份。（2026-09-15 補充，階段 2-1。原因：欄位規則變嚴後，本機資料一旦不符合契約，備份與檢查點都會被擋下，使用者需要保留資料的出口。）

### 5.5 檢查點與封存

- **檢查點**：內容就是該局的 gzip 備份（本文件 5.4 節），存在 `checkpointData`。回溯時先驗證備份，再以單一交易取代該局資料（12.4）。
- **封存**：封存檔也是同一種格式（12.3）。
- 備份、檢查點、封存共用同一套序列化與驗證程式。

### 5.6 版本遷移

- `schemaVersion` 是整數，從 1 開始，IndexedDB 版本與備份格式共用同一個號碼。
- 每次結構改版提供：
  1. 把第 N 版 `collections` 轉成第 N+1 版的純函式。
  2. IndexedDB 升級時資料表與索引的調整；本機資料的轉換呼叫同一個純函式。
- 舊版備份與檢查點依序逐版遷移，並寫入一筆遷移事件（DATA-05）。比目前程式新的版本一律拒絕（DATA-04）。
- `appVersion` 是程式版本，與 `schemaVersion` 分開；只改介面不升結構版本。
- 2026-09-15 補充（階段 2-1）：階段 2 期間 `schemaVersion` 維持 1。已經有欄位規則的資料表，合併後只能新增選填欄位或新的列舉值；新增必填欄位、收緊既有規則或改變欄位意義時，必須升級結構版本並提供遷移。較舊的程式讀取較新程式產生、含新欄位值或列舉值的備份時，會以欄位錯誤（`recordInvalid`）拒絕，而不是未來版本（`futureVersion`）。

## 6. 領域用語對照（D-09）

### 6.1 原則

1. 一個規格名詞只對應一個程式名稱，不使用同義詞。
2. 遊戲專有、英文意譯容易失真的名詞使用羅馬拼音或遊戲原縮寫，例如仔出 `kodashi`、`sp`、`st`。
3. 狀態值使用明確的字串列舉，不使用數字代碼。
4. 本節的名稱以 TypeScript 型別落實；型別與本節不一致時，以本節為準修正程式並回報。

### 6.2 必須嚴格區分的組合

| 規格用語 | 程式名稱 | 不得混用的對象 |
| --- | --- | --- |
| 自身父系所屬 | `horse.sireSubsystem` | 母馬群 `mare.group`：市場母馬的父系與她放在哪一群無關 |
| 母馬群 | `mare.group`，種類 `own`、`substitute`、`starter`、`unassigned` | 同上 |
| 子系統、親系統 | `subsystem`、`parentSystem` | 系位置 `position`（1～8）：系名改變時位置不變 |
| 能力番号 | `abilityNo` | 馬番号 `stageNumbers[]`：只記在階段歷程，不作比對 |
| 姊妹接替的「候選」 | `succession: 'sisterCandidate'` | 候選 TXT（匯入類型 `candidateFile`） |
| 市場補血、斷血補系 | `replenish`、`recovery` | 補血是常態，補系是例外 |
| 八系指定配種、自由配種 | `breedingType: 'designated' \| 'free'` | 建系期依 7.3 的配種也是 `designated` |
| 代數、產出代數 | `generation`（0＝零代）、`targetGeneration` | 「零代、初代、二代」等文字只在介面轉換 |

### 6.3 完整對照

**遊戲局與保存**

| 規格用語 | 程式名稱 |
| --- | --- |
| 遊戲局 | `Game`、`gameId` |
| 目前遊戲年、起始年 | `currentYear`、`startYear` |
| 備份 | `backup` |
| 檢查點、釘選、回溯 | `Checkpoint`、`pinned`、`rollback` |
| 封存 | `archive` |
| 結構版本、程式版本 | `schemaVersion`、`appVersion` |

**系與配對**

| 規格用語 | 程式名稱 |
| --- | --- |
| 第 N 系、系位置 | `position`（1～8）；資料表 `lines` |
| 代表色 | `color` |
| 系統對照表 | `systemMap` |
| 建系期、循環期 | `phase: 'building' \| 'cycling'` |
| 建系分支；推進原系、建立新系 | `branch`；`advance`、`found` |
| 零代市場種牡馬（建系或補系用） | `founder` |
| 配對距離 | `pairDistance`（1、2、4） |
| 任務 | `task` |
| 活血種數 | `activationCount`（0～8） |
| 4 代內重複的馬 | `duplicateAncestors` |
| 血統資料不足 | `insufficientPedigree` |
| 八系親系統種類數 | `parentSystemCount` |

**馬匹身分**

| 規格用語 | 程式名稱 |
| --- | --- |
| 內部識別 | `id` |
| 能力番号 | `abilityNo` |
| 馬番号 | `stageNumbers[]`，每筆 `{ stage, number, gameYear, source }` |
| 生命階段 | `stage: 'foal' \| 'racehorse' \| 'broodmare' \| 'stallion'` |
| 出生年、匯出年 | `birthYear`、`exportYear` |
| 性別（牡、牝） | `sex: 'male' \| 'female'` |
| 完整馬名、基本馬名、正式馬名 | `fullName`、`baseName`、`officialName` |
| 追蹤名（推導） | `trackingName` |
| 名稱別名 | `aliases[]` |
| 父馬、母馬 | `sireId`／`sireName`、`damId`／`damName`（內部 `id`／外部名稱） |
| 外部參考節點 | `externalRef` |
| 自身父系所屬 | `sireSubsystem` |
| 牝系 | `femaleLine` |

**繁殖牝馬**

| 規格用語 | 程式名稱 |
| --- | --- |
| 繁殖牝馬 | `Mare` |
| 繁殖牝馬圈 | `herd` |
| 母馬群 | `group`：`{ kind: 'own' \| 'substitute' \| 'starter', position, generation }` 或 `{ kind: 'unassigned' }` |
| 在圈狀態、離圈原因 | `status: 'producing' \| 'left'`、`leftReason: 'sold' \| 'retired'` |
| 來源：市場創系、市場補血、市場混血、市場補系、所屬競走馬引退轉入、其他 | `origin: 'marketFound' \| 'marketReplenish' \| 'marketMixed' \| 'marketRecovery' \| 'ownRetired' \| 'other'` |
| 來源備註 | `originNote` |
| 據點 | `site: 32 \| 33 \| 34 \| 35` |
| 繋養牧場番号、馬主番号（匯入欄） | `farmNo`、`ownerNo` |
| 轉場 | `transfer` |
| 姊妹接替：暫定保留、候選、正式保留、已被取代、已售出 | `succession: 'provisional' \| 'sisterCandidate' \| 'confirmed' \| 'replaced' \| 'sold'` |
| 今年計畫：待定、八系指定配種、自由配種、等待活力、輪休 | `yearPlan: { plan: 'undecided' \| 'designated' \| 'free' \| 'waitVitality' \| 'rest', gameYear }` |
| 活力、増強 | `vitality`、`boosted` |
| 仔出 | `kodashi` |
| 繁殖年數、繁殖頭數 | `breedingYears`、`breedingCount` |
| 定年、高齡提醒年齡、活力建議門檻 | `retirementAge`、`highAgeReminderAge`、`vitalityThreshold` |

**種牡馬**

| 規格用語 | 程式名稱 |
| --- | --- |
| 種牡馬任期 | `StallionDuty` |
| 現任、預定後繼 | `role: 'current' \| 'planned'` |
| 在崗、已被取代、退出生產行列、已引退 | `dutyStatus: 'onDuty' \| 'replaced' \| 'outOfService' \| 'retired'` |
| 預定後繼就緒狀態：尚未誕生、競走中、已引退待指定、正式供用 | `readiness: 'unborn' \| 'racing' \| 'retiredPending' \| 'inService'` |
| 更換原因：弟弟較優、前任引退、無法供用、斷血補系、遊戲依史實引退、其他 | `replaceReason: 'betterBrother' \| 'predecessorRetired' \| 'unavailable' \| 'recovery' \| 'historicalRetirement' \| 'other'` |
| 種牡馬提醒年齡 | `stallionAgeReminderAge` |
| 非現役（五月總表缺席） | `inactive` |
| 種付料 | `studFee` |

**配種與產駒**

| 規格用語 | 程式名稱 |
| --- | --- |
| 年度繁殖紀錄 | `Breeding` |
| 配種類型 | `breedingType: 'designated' \| 'free'` |
| 受胎狀態 | `conception: '空胎' \| '受胎' \| '不受胎' \| '未確認'` |
| 預定生產年 | `expectedBirthYear` |
| 規則快照 | `ruleSnapshot` |
| 血統檢查結果、確認紀錄 | `pedigreeCheck`、`confirmations` |
| 偏離規則 | `deviated` |
| 輪空（推導） | `idle` |
| 配種評價；總合評價；爆發力 | `MatingRating`；`overallGrade: 'S' \| 'A' \| 'B' \| 'C' \| 'D'`；`explosivePower` |
| 產駒；產駒的系與代數 | `Foal`；`lineage: { position, generation }` |
| SP、ST | `sp`、`st` |
| 副能力：力量、瞬發力、勝負根性、柔軟性、精神力、賢明度、健康 | `subParams: { power, quickness, guts, flexibility, spirit, wisdom, health }`，等級 `'G'`～`'S+'` |
| サ | `subParamTotal` |
| 芝、ダート適性 | `turf`、`dirt`：`'◎' \| '○' \| '△' \| '×'` |
| 場地型摘要（推導） | `surfaceSummary` |
| 距離適性 | `distanceText` |
| 牧場處置：保留、待售、已售出 | `disposition: 'keep' \| 'forSale' \| 'sold'` |
| 自由配種產駒 | `freeBred` |
| 自家產駒的去向：成為種牡馬、在其他牧場成為繁殖牝馬 | `fate: 'becameStallion' \| 'mareElsewhere'`；後者含 `farmNo`、`lastSeenYear` |

**匯入**

| 規格用語 | 程式名稱 |
| --- | --- |
| 匯入批次 | `ImportBatch` |
| 匯入類型：一月二歲馬、四月誕生幼駒、五月繁殖牝馬、七月繁殖牝馬、候選 TXT、五月種牡馬、目標種牡馬 TXT、十月全世界繁殖牝馬 | `ImportType: 'jan2yo' \| 'aprFoals' \| 'mayMares' \| 'julMares' \| 'candidateFile' \| 'mayStallions' \| 'targetStallion' \| 'octWorldMares'` |
| 時點 | `timing: { month, week }` |
| 預覽分類：可套用、略過、待核對、警告、錯誤 | `apply`、`skip`、`review`、`warn`、`error` |
| 5.2 的處理：阻止、警告並確認、停止套用 | `block`、`confirm`、`halt` |
| 重複、資料更正 | `duplicate`、`correction` |

## 7. 測試分層與合成樣本（D-03、D-04、D-05、D-10）

### 7.1 測試層級

| 層級 | 工具與環境 | 範圍 |
| --- | --- | --- |
| 單元 | Vitest，Node | `domain` 純函式、`import` 解析器 |
| 整合 | Vitest ＋ fake-indexeddb，Node | 應用服務＋資料庫：交易失敗完整退回、唯一索引、備份往返、版本遷移、檢查點回溯。每個測試使用全新資料庫 |
| 端到端 | Playwright，以 `file://` 開啟 `app/dist/WPStudBook.html` | 介面流程、重新整理後資料仍在、下載與匯入。本機只選用 Edge（`playwright.config.ts` 預設 `E2E_BROWSERS=msedge`），未選用 Chrome 時列印警告；GitHub Actions 的 Windows 環境跑 Edge 與 Chrome。被選用的瀏覽器若未安裝，測試直接失敗，不會標示略過 |
| 模擬 | Vitest，Node | 階段 3 八系多代推演 |
| 效能 | Playwright ＋ 大量合成資料 | 階段 5 量測並記錄，不設硬性門檻 |

### 7.2 指令

以下指令都在 `app/` 內執行。

| 指令 | 內容 |
| --- | --- |
| `npm run lint` | ESLint 與 Prettier 檢查 |
| `npm run typecheck` | TypeScript 型別檢查（`src` 與 Node 端分開檢查） |
| `npm test` | 單元、整合與模擬測試 |
| `npm run build` | 建置 `app/dist/WPStudBook.html`，並檢查成品沒有外部載入（BLD-02） |
| `npm run test:e2e` | 建置後執行端到端測試 |
| `npm run e2e` | 對既有建置檔執行端到端測試，不含 `@references`；CI 與發布流程使用 |
| `npm run test:references` | 本機實檔測試（本文件 7.4 節） |
| `npm run scenarios` | 列出規格第 15 章尚未被任何測試標題涵蓋的情境代號 |
| `npm run check` | 依序執行 lint、typecheck、test、build、test:e2e（計畫階段 0「一個指令完成建置與全部測試」） |

> 2026-09-15 補充：端到端測試列的瀏覽器略過說明與本表 `npm run e2e` 行，依實作校正（見 `app/playwright.config.ts`、`.github/workflows/ci.yml`）。

### 7.3 情境代號與測試

- 測試標題以情境代號開頭，例如 `[LINE-14] 產出 5 代用 1↔2…`；一個情境可由多個測試共同證明，可用 `vitest -t LINE-14` 單獨執行。
- 更新驗收追蹤時，以 `npm run scenarios` 的結果與測試執行紀錄為依據。

### 7.4 合成樣本與實檔測試

- **產生器**：`app/scripts/` 內的 TypeScript 產生器依附錄 A 的欄數與欄位位置組出 TSV，再以 iconv-lite 轉成 CP932。
- **內容**：全部虛構（例如 `テストウマ001`），涵蓋 `(外)`、`[地]` 前綴、`0x0000`、空白欄、`*13`、`72(72)`、`42( +0)`、欄位內含逗號的 `2,450`、父系結尾「系」、年與月之間沒有空白的檔名。
- **不提交產物**：測試執行時產生到 `app/tests/fixtures/generated/`，版本控制只放產生器與設定；5,000 筆效能樣本同樣臨時產生。
- **實檔測試**：解析器與產生器共用同一份欄位位置表，為避免兩邊一起錯，`npm run test:references` 以 repo 根目錄 `.references/`（從 `app/` 讀取為 `../.references/`）的真實樣本驗證表頭與欄位位置。檔案不存在時結果記為「未執行」；只輸出筆數與比對結果，不輸出原始資料；CI 不執行。

### 7.5 階段 0 必須驗證的環境條件

在 Edge 以 `file://` 開啟建置檔，逐項確認；任一項失敗時先回到本文件調整方案，不進入階段 1：

1. 內嵌的 module script 可以執行。
2. `window.isSecureContext` 為真，`crypto.randomUUID()`、`crypto.subtle.digest('SHA-256')`、`CompressionStream` 與 `DecompressionStream` 可以使用。
3. `new TextDecoder('shift_jis', { fatal: true })` 可以解碼 CP932，並與實檔測試結果一致。
4. IndexedDB 寫入後重新整理，資料仍可讀回。
5. 建置檔沒有任何外部載入，並記錄檔案大小。
6. GitHub Actions 的 Windows 環境有 Edge 與 Chrome，兩者的端到端測試都通過（計畫關卡 A）。

**驗證結果（階段 0）**

| 項目 | 結果 |
| --- | --- |
| 驗證日期 | 2026-09-15 |
| 本機 Edge | 153.0.4234.32，任務 4 端到端 3 個測試通過、任務 5 `@references` 6 個測試通過 |
| GitHub Actions | https://github.com/AxeenWang/WPStudBook/actions/runs/34876286193 （PR #1）；檢查與建置成功，端到端 msedge 3 passed、chrome 3 passed（windows-latest 預載瀏覽器） |
| 內嵌 module script、安全環境 API、gzip、CP932 解碼、IndexedDB 重新整理 | 全部通過 |
| Edge 與 Node 解碼六份實檔 | 結果一致 |
| 成品大小 | 258.8 KB |

### 7.6 `file://` 的 IndexedDB 共用範圍（階段 1 驗證）

2026-09-15 補充。原因：需求規格 12.2 原本提醒「搬移 HTML 會看不到原資料」，階段 1 收尾前以實際瀏覽器驗證，結果不成立；已依使用者裁定更正需求規格 12.2 與頂部狀態列的提醒。

方法：以 Playwright 驅動本機 Edge（headless），把同一份建置檔複製到兩個不同資料夾，另寫一個不含 WPStudBook 程式、檔名與資料夾都不同的本機 HTML，全部以 `file://` 開啟；先在同一個瀏覽器 context 內比對，再開一個新的 context 對照。

| 項目 | 結果 |
| --- | --- |
| 驗證日期 | 2026-09-15 |
| 本機 Edge | 153.0.4234.32（Playwright `msedge` channel） |
| `location.origin`／`self.origin` | 每個頁面都是 `"file://"`／`"null"`，與檔名、資料夾無關 |
| 同一建置檔放在兩個資料夾，同一 context | 第二份的頂部狀態列與直接讀取資料庫，都看到第一份建立的遊戲局（同一筆 `id`） |
| 無關的本機 HTML，同一 context | `indexedDB.databases()` 列出 `wpstudbook`，可開啟並讀出全部資料表名稱與遊戲局紀錄 |
| 新的瀏覽器 context | 看不到上述遊戲局；資料庫是該 context 開啟程式時自己建立的空資料庫 |

結論：

- Edge 的 `file://` 頁面在同一瀏覽器設定檔內共用一個儲存來源，IndexedDB 不依檔名或資料夾分開。搬移、複製或改名 HTML 檔仍看得到原資料；換到其他設定檔（本次以新的 browser context 代表）則看不到。
- 同一設定檔中開啟的任何本機 HTML 都能開啟並讀取 `wpstudbook` 資料庫。本次只驗證讀取；寫入受同一個儲存來源規則管轄，推定同樣可行。
- 限制：不同設定檔的隔離以 Playwright 新的 browser context 代表，沒有實際切換 Edge 設定檔；Chrome 同為 Chromium 核心，本次沒有另外驗證。

## 8. 版本號、分支與 CI（D-11、D-12）

### 8.1 版本號

- `appVersion`：開發期間 `0.x.y`，完成一個計畫階段升 x、修正升 y；符合計畫第 10 章第一版完成定義時發布 `1.0.0`。建置時由 `package.json` 帶入，頁面與備份檔都可看到。
- `schemaVersion`：本文件 5.6 節，只在資料結構改變時升級。

### 8.2 分支流程

- 分支名稱沿用「主題_YYYYMMDD」，例如 `source_20260914`。
- 程式與文件修改一律走：主題分支 → `/repo-push` → `/repo-open-pr` → CI 通過 → 合併 → `/repo-clean-branch`，不直接推送 `main`。變更只涉及 `docs/` 或 Markdown 檔的 PR 會跳過端到端測試。
- 合併使用 GitHub 預設的 merge commit，保留每個 commit，不改寫歷史。
- 「CI 通過才能合併」的分支保護屬於 repo 設定，需使用者另外同意後才設定。

### 8.3 CI（GitHub Actions）

| 工作流程 | 觸發 | 內容 |
| --- | --- | --- |
| `ci.yml` 檢查 | PR、推送到 `main` | Ubuntu：`npm ci` → lint → typecheck → 單元與整合測試 → 建置 → 檢查成品沒有外部載入 → 保存建置檔 |
| `ci.yml` 端到端 | 檢查通過後；變更只涉及 `docs/` 或 Markdown 檔時跳過 | Windows：取用上一步的建置檔，以 Playwright 驅動 Edge 與 Chrome 各執行一次；失敗時保留 trace、截圖與報告 |
| `release.yml` 發布 | 推送 `v*` 標籤 | 重跑建置與測試 → 建立 GitHub Release 並附上 `WPStudBook.html` |

- 工作流程預設只有讀取權限，只有發布流程可以寫入 Release。
- 工作流程以 `app/` 為指令的預設工作目錄；上傳與下載建置檔的路徑為 `app/dist/`。
- 2026-09-15 修正：`ci.yml` 端到端失敗時原訂保留「錄影」，實作已停用影片錄製並改為 trace 與失敗自動截圖，因 Playwright 影片錄製需要另外下載 ffmpeg 執行檔，而本計畫不下載額外執行檔（見 `app/playwright.config.ts`）；上表已同步修正。
- Node 版本固定為 24，依賴以 `app/package-lock.json` 鎖定。
- CI 只使用合成樣本，不接觸 `.references/`。
- repo 根目錄的 `.gitignore` 加入 `node_modules/`、`dist/`、`test-results/`、`playwright-report/`、`tests/fixtures/generated/`；規則不帶前導斜線，因此涵蓋 `app/` 內的產出。
- 2026-09-15 補充：第三方 action 一律以 commit SHA 釘選並在行尾註明版本，升級時同步更新兩個工作流程；`release.yml` 先確認標籤指向 `main` 上的 commit 才建置，端到端失敗時同樣保留報告。
