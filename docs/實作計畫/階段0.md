# 階段 0：設計與工具鏈 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目標**：建立可建置、可自動測試的專案骨架，並證明單一 HTML 成品在 `file://` 下符合[設計決策](../設計決策.md) 7.5 節的全部環境條件。

**架構**：程式專案全部放在 `app/`，Vite 把 React 程式與樣式內嵌成 `app/dist/WPStudBook.html`；程式碼分為 `domain`、`import`、`storage`、`services`、`ui` 五層，由 ESLint 強制引用規則。畫面只放一個「環境檢查」面板，逐項回報 `file://` 下的安全環境 API、CP932 解碼與 IndexedDB 持久化結果，由 Playwright 驅動 Edge（本機）與 Chrome（GitHub Actions）驗證。

**技術組合**：Node 24、npm、TypeScript 6.0.3、Vite 8.3.0、vite-plugin-singlefile 2.3.3、React 19.3.0、react-aria-components 1.21.1、idb 8.0.3、Vitest 5.0.0、fake-indexeddb 6.2.5、@playwright/test 1.63.0、ESLint 10.10.0、typescript-eslint 8.70.0、Prettier 3.9.6、GitHub Actions。

**依據**：[開發計畫](../開發計畫.md) 階段 0、[設計決策](../設計決策.md)。本計畫與設計決策衝突時以設計決策為準並回報。

## 全域限制

- repo 根目錄 `D:\AxeenWrold\CodeLab\Projects\WPStudBook` 只放專案規則、`.gitignore`、`.github/`、`docs/`、`.references/` 與 Agent 暫存區（專案 `AGENTS.md` 的 Repository Layout、設計決策 4.1 節）。程式、設定、依賴與建置測試產出一律放在 `app/`；npm、建置、測試與程式檢查指令都在 `app/` 內執行，禁止在根目錄執行 `npm install` 或建立工具設定。
- 本計畫的檔案路徑與指令，除特別標明「repo 根目錄」外，都相對於 `app/`。
- 分支 `source_20260914`；Shell 使用 Git Bash。
- Node 24 以上；TypeScript 固定 6.0.3，不升級到 7.x（typescript-eslint 8.70 只支援 `>=4.8.4 <6.1.0`）。
- 所有套件以 `--save-exact` 安裝確切版本。`npm install` 會從 npm registry 下載套件：每個包含安裝的步驟，執行前先在對話中列出套件並取得使用者同意。
- TypeScript 設定必須包含 `strict`、`exactOptionalPropertyTypes`、`noUncheckedIndexedAccess`、`erasableSyntaxOnly`。
- 相對引用一律寫出副檔名（`.ts`、`.tsx`、`.js`），讓 Vite、Vitest 與 Node 直接執行 TypeScript 腳本時使用同一套寫法。
- 成品只有 `app/dist/WPStudBook.html` 一個檔案，不得有任何外部載入（規格 BLD-02）。
- 分層規則依設計決策第 4 節：`domain` 不引用其他層、React、`idb` 與瀏覽器全域物件；`ui` 不引用 `storage`、`import`、`idb`，對 `domain` 只能 `import type`。
- `.references/`（repo 根目錄；從 `app/` 讀取為 `../.references/`）的內容不得寫進任何受版本控制的檔案；實檔測試只輸出檔名、筆數與比對結果。
- 需要暫存檔時放在 repo 根目錄的 `.claude-tmp/2026-09-14_stage0/`，不得提交。
- commit 訊息使用繁體中文，結尾加上 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。
- 推送與開 PR 屬於對外動作，執行前取得使用者同意；本計畫各任務只做本機 commit，推送集中在任務 7。

## 不在本計畫範圍

- IndexedDB 結構第 1 版、備份與檢查點（階段 1）。本計畫的探測資料庫 `wpstudbook-probe` 與正式資料庫分開。
- 合成樣本產生器與 iconv-lite（階段 4-1，與解析器一起做）。
- @tanstack/react-virtual（第一個長清單出現時再安裝）。
- 設定分支保護、發布第一個 Release（需使用者另外同意）。

## 檔案結構

以下路徑相對於 `app/`；`.github/workflows/` 在 repo 根目錄。

| 檔案 | 責任 |
| --- | --- |
| `package.json`、`package-lock.json` | 依賴版本與 npm 指令 |
| `tsconfig.json` | 單一 TypeScript 設定，涵蓋 `src`、`tests`、`scripts` 與根目錄設定檔 |
| `vite.config.ts` | 建置單一 HTML、注入程式版本 |
| `vitest.config.ts` | 測試專案 `unit`、`integration`、`references` |
| `playwright.config.ts` | 端到端測試，依 `E2E_BROWSERS` 選擇 Edge、Chrome |
| `eslint.config.js` | ESLint 主設定（型別感知檢查、React Hooks） |
| `eslint.layers.js` | 分層引用規則，主設定與測試共用 |
| `.prettierrc.json`、`.prettierignore` | 格式設定 |
| `WPStudBook.html` | 建置入口 HTML |
| `src/main.tsx` | 掛載 React |
| `src/env.d.ts` | 建置時注入的全域常數型別 |
| `src/ui/App.tsx` | 最上層畫面 |
| `src/ui/EnvironmentPanel.tsx` | 環境檢查面板 |
| `src/services/environment-check.ts` | 逐項執行環境檢查 |
| `src/storage/probe-counter.ts` | IndexedDB 探測計數 |
| `src/domain/import-type.ts` | 匯入類型列舉（設計決策 6.3 節） |
| `src/import/formats.ts` | 各匯入格式的欄數與關鍵表頭位置表、表頭檢查 |
| `scripts/lib/external-refs.ts` | 找出 HTML 內的外部載入 |
| `scripts/verify-dist.ts` | 建置後檢查成品 |
| `scripts/lib/scenario-coverage.ts` | 比對規格情境代號與測試標籤 |
| `scripts/scenarios.ts` | 輸出情境覆蓋清單 |
| `tests/unit/*.test.ts` | 單元測試 |
| `tests/integration/*.test.ts` | 整合測試（fake-indexeddb） |
| `tests/references/*` | 本機實檔測試 |
| `tests/e2e/*` | Playwright 端到端測試 |
| `.github/workflows/ci.yml`、`release.yml`（repo 根目錄） | GitHub Actions |

---

### 任務 1：專案骨架、單一 HTML 建置與外部載入檢查

> 已於 2026-09-14 在 repo 根目錄完成（commit `39e98e4`、`8725102`），由任務 1A 搬到 `app/`。以下保留原始步驟供追溯，路徑以當時的 repo 根目錄為準。

**Files:**
- Create: `package.json`、`tsconfig.json`、`vite.config.ts`、`vitest.config.ts`、`WPStudBook.html`、`src/main.tsx`、`src/env.d.ts`、`src/ui/App.tsx`、`scripts/lib/external-refs.ts`、`scripts/verify-dist.ts`
- Modify: `.gitignore`
- Test: `tests/unit/external-refs.test.ts`

**Interfaces:**
- Consumes：無。
- Produces：
  - `findExternalReferences(html: string): ExternalReference[]`，`ExternalReference = { kind: 'attribute' | 'css'; value: string }`
  - npm 指令 `build`（建置並執行 `verify-dist`）、`typecheck`、`test`
  - Vitest 專案名稱 `unit`、`integration`
  - 全域常數 `__APP_VERSION__: string`
  - `App` 元件（`src/ui/App.tsx`，具名匯出）

- [ ] **Step 1：建立 `package.json` 並安裝依賴**

建立 `package.json`：

```json
{
  "name": "wpstudbook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "vite build && node scripts/verify-dist.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --project integration"
  }
}
```

取得使用者同意後安裝：

```bash
npm install --save-exact react@19.3.0 react-dom@19.3.0
npm install --save-dev --save-exact typescript@6.0.3 vite@8.3.0 @vitejs/plugin-react@6.1.1 vite-plugin-singlefile@2.3.3 vitest@5.0.0 @types/node@24.13.4 @types/react@19.3.0 @types/react-dom@19.3.0
```

Expected：安裝完成，產生 `package-lock.json`。若出現 peer dependency 錯誤，停下來回報錯誤內容，不使用 `--force` 或 `--legacy-peer-deps`。

- [ ] **Step 2：建立 TypeScript、Vite、Vitest 設定與最小畫面**

`tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2024",
    "lib": ["ES2024", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "types": ["node"],
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "verbatimModuleSyntax": true,
    "erasableSyntaxOnly": true,
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src", "tests", "scripts", "*.ts", "*.js"]
}
```

`vite.config.ts`：

```ts
import { readFileSync } from 'node:fs';
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';
import { viteSingleFile } from 'vite-plugin-singlefile';

const packageJson = JSON.parse(readFileSync('package.json', 'utf8')) as { version: string };

export default defineConfig({
  plugins: [react(), viteSingleFile()],
  define: {
    __APP_VERSION__: JSON.stringify(packageJson.version),
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true,
    rolldownOptions: {
      input: 'WPStudBook.html',
    },
  },
});
```

`vitest.config.ts`：

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    projects: [
      {
        test: {
          name: 'unit',
          include: ['tests/unit/**/*.test.ts'],
          environment: 'node',
        },
      },
      {
        test: {
          name: 'integration',
          include: ['tests/integration/**/*.test.ts'],
          environment: 'node',
        },
      },
    ],
  },
});
```

`WPStudBook.html`：

```html
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WPStudBook</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/env.d.ts`：

```ts
declare const __APP_VERSION__: string;
```

`src/main.tsx`：

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './ui/App.tsx';

const container = document.getElementById('root');
if (container === null) {
  throw new Error('找不到 #root 容器');
}

createRoot(container).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`src/ui/App.tsx`：

```tsx
export function App() {
  return (
    <main>
      <h1>WPStudBook</h1>
      <p data-testid="app-version">版本 {__APP_VERSION__}</p>
    </main>
  );
}
```

- [ ] **Step 3：撰寫外部載入檢查的失敗測試**

`tests/unit/external-refs.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { findExternalReferences } from '../../scripts/lib/external-refs.ts';

describe('findExternalReferences', () => {
  it('內嵌的 script 與 style 不算外部載入，即使程式內容含有網址字串', () => {
    const html =
      '<html><head><style>body{background:url(data:image/png;base64,AAAA)}</style></head>' +
      '<body><script type="module">const markup = "<img src=\\"https://example.com/a.png\\">";</script></body></html>';
    expect(findExternalReferences(html)).toEqual([]);
  });

  it('script 的 src 指向檔案時回報', () => {
    const html = '<script type="module" src="/assets/index.js"></script>';
    expect(findExternalReferences(html)).toEqual([{ kind: 'attribute', value: '/assets/index.js' }]);
  });

  it('外部樣式表連結回報', () => {
    const html = '<link rel="stylesheet" href="https://cdn.example.com/site.css">';
    expect(findExternalReferences(html)).toEqual([
      { kind: 'attribute', value: 'https://cdn.example.com/site.css' },
    ]);
  });

  it('CSS 的 url() 與 @import 指向外部時回報', () => {
    const html =
      '<style>@import "https://cdn.example.com/base.css";' +
      '@font-face{src:url("fonts/a.woff2")}</style>';
    expect(findExternalReferences(html)).toEqual([
      { kind: 'css', value: 'fonts/a.woff2' },
      { kind: 'css', value: 'https://cdn.example.com/base.css' },
    ]);
  });

  it('錨點、data 與 blob 網址不算外部載入', () => {
    const html = '<a href="#top">頂端</a><img src="data:image/png;base64,AAAA"><a href="blob:abc">檔案</a>';
    expect(findExternalReferences(html)).toEqual([]);
  });

  it('沒有引號的屬性也能偵測', () => {
    expect(findExternalReferences('<img src=logo.png>')).toEqual([{ kind: 'attribute', value: 'logo.png' }]);
  });
});
```

- [ ] **Step 4：執行測試確認失敗**

Run：`npx vitest run --project unit tests/unit/external-refs.test.ts`
Expected：FAIL，錯誤指出找不到 `scripts/lib/external-refs.ts`。

- [ ] **Step 5：實作 `findExternalReferences`**

`scripts/lib/external-refs.ts`：

```ts
export interface ExternalReference {
  kind: 'attribute' | 'css';
  value: string;
}

const SCRIPT_BLOCK = /<script\b([^>]*)>[\s\S]*?<\/script>/gi;
const STYLE_BLOCK = /<style\b([^>]*)>([\s\S]*?)<\/style>/gi;
const URL_ATTRIBUTE = /\b(?:src|href|srcset|poster|action)\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s"'>]+))/gi;
const CSS_URL = /url\(\s*(?:"([^"]*)"|'([^']*)'|([^)\s]*))\s*\)/gi;
const CSS_IMPORT_STRING = /@import\s+(?:"([^"]+)"|'([^']+)')/gi;

function isEmbedded(value: string): boolean {
  const normalized = value.trim().toLowerCase();
  return (
    normalized === '' ||
    normalized.startsWith('#') ||
    normalized.startsWith('data:') ||
    normalized.startsWith('blob:')
  );
}

export function findExternalReferences(html: string): ExternalReference[] {
  const references: ExternalReference[] = [];
  const cssBlocks: string[] = [];

  const markup = html
    .replace(SCRIPT_BLOCK, (_match: string, attributes: string) => `<script${attributes}></script>`)
    .replace(STYLE_BLOCK, (_match: string, attributes: string, css: string) => {
      cssBlocks.push(css);
      return `<style${attributes}></style>`;
    });

  for (const match of markup.matchAll(URL_ATTRIBUTE)) {
    const value = match[1] ?? match[2] ?? match[3] ?? '';
    if (!isEmbedded(value)) {
      references.push({ kind: 'attribute', value });
    }
  }

  for (const css of cssBlocks) {
    for (const match of css.matchAll(CSS_URL)) {
      const value = match[1] ?? match[2] ?? match[3] ?? '';
      if (!isEmbedded(value)) {
        references.push({ kind: 'css', value });
      }
    }
    for (const match of css.matchAll(CSS_IMPORT_STRING)) {
      const value = match[1] ?? match[2] ?? '';
      if (!isEmbedded(value)) {
        references.push({ kind: 'css', value });
      }
    }
  }

  return references;
}
```

- [ ] **Step 6：執行測試確認通過**

Run：`npx vitest run --project unit tests/unit/external-refs.test.ts`
Expected：PASS，6 個測試通過。

- [ ] **Step 7：建立成品檢查腳本並建置**

`scripts/verify-dist.ts`：

```ts
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join } from 'node:path';
import { findExternalReferences } from './lib/external-refs.ts';

const DIST_DIR = 'dist';
const OUTPUT_FILE = 'WPStudBook.html';

const entries = readdirSync(DIST_DIR);
if (entries.length !== 1 || entries[0] !== OUTPUT_FILE) {
  console.error(`dist/ 應只有 ${OUTPUT_FILE}，實際為：${entries.join(', ')}`);
  process.exit(1);
}

const filePath = join(DIST_DIR, OUTPUT_FILE);
const references = findExternalReferences(readFileSync(filePath, 'utf8'));
if (references.length > 0) {
  console.error('成品含有外部載入：');
  for (const reference of references) {
    console.error(`- [${reference.kind}] ${reference.value}`);
  }
  process.exit(1);
}

const sizeKb = (statSync(filePath).size / 1024).toFixed(1);
console.log(`dist/${OUTPUT_FILE} 通過外部載入檢查，大小 ${sizeKb} KB`);
```

Run：`npm run build`
Expected：Vite 建置成功，最後一行輸出 `dist/WPStudBook.html 通過外部載入檢查，大小 …… KB`。

若 `dist/` 出現其他檔案（例如 `index.html` 或 `assets/`），代表入口名稱或單檔插件沒有生效：先確認 `rolldownOptions.input` 是 `WPStudBook.html`，再確認 vite-plugin-singlefile 與 Vite 8 相容；無法解決時停下來回報，不改用其他建置方式。

- [ ] **Step 8：型別檢查與全部單元測試**

Run：`npm run typecheck`
Expected：沒有錯誤。

Run：`npm test`
Expected：`unit` 專案 6 個測試通過；`integration` 專案沒有測試檔（Vitest 不應因此失敗；若失敗，在 `vitest.config.ts` 的根層 `test` 加上 `passWithNoTests: true` 後重跑）。

- [ ] **Step 9：更新 `.gitignore`**

在既有 `.gitignore` 末尾加入：

```gitignore

# Build and test outputs
node_modules/
dist/
test-results/
playwright-report/
tests/fixtures/generated/
```

Run：`git status --short`
Expected：列出本任務新增的檔案與 `.gitignore`，不含 `node_modules/`、`dist/`。

- [ ] **Step 10：Commit**

```bash
git add .gitignore package.json package-lock.json tsconfig.json vite.config.ts vitest.config.ts WPStudBook.html src scripts tests
git commit -m "建立專案骨架與單一 HTML 建置" -m "Vite 8 以 vite-plugin-singlefile 輸出 dist/WPStudBook.html，建置後檢查成品沒有外部載入。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 1A：將程式專案搬到 app/

**Files:**
- Move（`git mv`，repo 根目錄 → `app/`）：`package.json`、`package-lock.json`、`tsconfig.json`、`vite.config.ts`、`vitest.config.ts`、`WPStudBook.html`、`src/`、`scripts/`、`tests/`
- Move（不進版本控制）：`node_modules/` → `app/node_modules/`
- Delete（建置產出，會重新產生）：repo 根目錄的 `dist/`

**Interfaces:**
- Consumes：任務 1 的全部檔案與 npm 指令（`build`、`typecheck`、`test`）。
- Produces：程式專案根目錄改為 `app/`；之後所有任務以 `app/` 為工作目錄。repo 根目錄的 `.gitignore` 規則不帶前導斜線，已涵蓋 `app/` 內的產出，不需修改。

- [ ] **Step 1：確認起點乾淨**

Run（repo 根目錄）：`git status --short`
Expected：沒有輸出。

- [ ] **Step 2：搬移已提交的檔案**

Run（repo 根目錄）：

```bash
mkdir app
git mv package.json package-lock.json tsconfig.json vite.config.ts vitest.config.ts WPStudBook.html src scripts tests app/
git status --short
```

Expected：只列出重新命名（`R`）的項目，沒有新增或刪除。

- [ ] **Step 3：搬移依賴並刪除舊建置產出**

Run（repo 根目錄）：

```bash
mv node_modules app/
rm -rf dist
```

若 `mv` 因檔案鎖定失敗：刪除 repo 根目錄殘留的 `node_modules/`，在 `app/` 內執行 `npm ci`（版本與 `package-lock.json` 相同，屬使用者已同意的安裝），不得安裝其他套件。

- [ ] **Step 4：在 app/ 內驗證**

Run（`app/`）：`npm run typecheck && npm test && npm run build`
Expected：型別檢查沒有錯誤；`unit` 專案 9 個測試通過；建置最後一行為 `dist/WPStudBook.html 通過外部載入檢查，大小 …… KB`，產物位於 `app/dist/`。

- [ ] **Step 5：確認 repo 根目錄**

Run（repo 根目錄）：`ls -A`
Expected：只有 `.claude-tmp`、`.git`、`.gitignore`、`.references`、`AGENTS.md`、`AGENTS_cht.md`、`CLAUDE.md`、`app`、`docs`。

Run（repo 根目錄）：`git status --short --ignored`
Expected：被忽略的產出只出現在 `app/`（`app/node_modules/`、`app/dist/`）與 `.claude-tmp/`、`.references/`。

- [ ] **Step 6：Commit**

```bash
git commit -m "將程式專案搬到 app/ 子資料夾" -m "repo 根目錄只保留專案規則、文件與參考資料；程式、設定、依賴與建置產出集中在 app/。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 2：ESLint 分層規則與 Prettier

**Files:**
- Create: `eslint.layers.js`、`eslint.config.js`、`.prettierrc.json`、`.prettierignore`
- Modify: `package.json`（scripts）
- Test: `tests/unit/lint-layers.test.ts`

**Interfaces:**
- Consumes：任務 1 的 `tsconfig.json`、Vitest `unit` 專案。
- Produces：
  - `layerConfigs`（`eslint.layers.js` 具名匯出，型別 `import('eslint').Linter.Config[]`）
  - npm 指令 `lint`、`format`

- [ ] **Step 1：安裝 ESLint 與 Prettier**

取得使用者同意後執行：

```bash
npm install --save-dev --save-exact eslint@10.10.0 @eslint/js@10.0.1 typescript-eslint@8.70.0 eslint-plugin-react-hooks@7.1.1 prettier@3.9.6
```

Expected：安裝完成，沒有 peer dependency 錯誤。

- [ ] **Step 2：撰寫分層規則的失敗測試**

`tests/unit/lint-layers.test.ts`：

```ts
import { ESLint } from 'eslint';
import { describe, expect, it } from 'vitest';
import { layerConfigs } from '../../eslint.layers.js';

async function lintRuleIds(filePath: string, code: string): Promise<string[]> {
  const eslint = new ESLint({ overrideConfigFile: true, overrideConfig: layerConfigs });
  const results = await eslint.lintText(code, { filePath });
  return results.flatMap((result) => result.messages.map((message) => message.ruleId ?? 'fatal'));
}

describe('分層規則（設計決策第 4 節）', () => {
  it('domain 不可引用 React', async () => {
    const ids = await lintRuleIds(
      'src/domain/probe.ts',
      "import { useState } from 'react';\nexport const hook = useState;\n",
    );
    expect(ids).toContain('@typescript-eslint/no-restricted-imports');
  });

  it('domain 不可使用瀏覽器全域物件', async () => {
    const ids = await lintRuleIds('src/domain/probe.ts', 'export const place = window.location;\n');
    expect(ids).toContain('no-restricted-globals');
  });

  it('ui 不可引用 storage', async () => {
    const ids = await lintRuleIds(
      'src/ui/Probe.tsx',
      "import { openProbe } from '../storage/probe.ts';\nexport const probe = openProbe;\n",
    );
    expect(ids).toContain('@typescript-eslint/no-restricted-imports');
  });

  it('ui 可以用 import type 引用 domain', async () => {
    const ids = await lintRuleIds(
      'src/ui/Probe.tsx',
      "import type { Horse } from '../domain/horse.ts';\nexport type ProbeHorse = Horse;\n",
    );
    expect(ids).toEqual([]);
  });

  it('ui 不可引用 domain 的值', async () => {
    const ids = await lintRuleIds(
      'src/ui/Probe.tsx',
      "import { rule } from '../domain/rule.ts';\nexport const probe = rule;\n",
    );
    expect(ids).toContain('@typescript-eslint/no-restricted-imports');
  });

  it('services 可以引用 storage', async () => {
    const ids = await lintRuleIds(
      'src/services/probe.ts',
      "import { openProbe } from '../storage/probe.ts';\nexport const probe = openProbe;\n",
    );
    expect(ids).toEqual([]);
  });

  it('storage 不可引用 services', async () => {
    const ids = await lintRuleIds(
      'src/storage/probe.ts',
      "import { runCheck } from '../services/check.ts';\nexport const probe = runCheck;\n",
    );
    expect(ids).toContain('@typescript-eslint/no-restricted-imports');
  });
});
```

- [ ] **Step 3：執行測試確認失敗**

Run：`npx vitest run --project unit tests/unit/lint-layers.test.ts`
Expected：FAIL，錯誤指出找不到 `eslint.layers.js`。

- [ ] **Step 4：實作分層規則**

`eslint.layers.js`：

```js
// @ts-check
import tseslint from 'typescript-eslint';

const LAYER_MESSAGE = '違反分層規則（設計決策第 4 節）';

/**
 * @param {string} layer
 * @param {boolean} [allowTypeImports]
 */
function layerImport(layer, allowTypeImports = false) {
  return {
    regex: `^(\\.{1,2}/)+(.*/)?${layer}(/|$)`,
    allowTypeImports,
    message: `${LAYER_MESSAGE}：不可引用 ${layer} 層${allowTypeImports ? '的值，只能 import type' : ''}`,
  };
}

const REACT_PACKAGES = {
  group: ['react', 'react/*', 'react-dom', 'react-dom/*', 'react-aria-components', '@tanstack/react-virtual'],
  message: `${LAYER_MESSAGE}：只有 ui 層可以使用 React`,
};

const IDB_PACKAGE = {
  group: ['idb'],
  message: `${LAYER_MESSAGE}：此層不可使用 idb`,
};

const BROWSER_GLOBALS = [
  'window',
  'document',
  'navigator',
  'location',
  'indexedDB',
  'localStorage',
  'sessionStorage',
  'fetch',
  'crypto',
  'CompressionStream',
  'DecompressionStream',
].map((name) => ({ name, message: `${LAYER_MESSAGE}：domain 層不可使用瀏覽器全域物件` }));

/** @type {import('eslint').Linter.Config[]} */
export const layerConfigs = [
  {
    name: 'wpstudbook/layers/setup',
    files: ['src/**/*.{ts,tsx}'],
    languageOptions: { parser: tseslint.parser },
    plugins: { '@typescript-eslint': tseslint.plugin },
  },
  {
    name: 'wpstudbook/layers/domain',
    files: ['src/domain/**/*.{ts,tsx}'],
    rules: {
      'no-restricted-globals': ['error', ...BROWSER_GLOBALS],
      '@typescript-eslint/no-restricted-imports': [
        'error',
        {
          patterns: [
            layerImport('import'),
            layerImport('storage'),
            layerImport('services'),
            layerImport('ui'),
            REACT_PACKAGES,
            IDB_PACKAGE,
          ],
        },
      ],
    },
  },
  {
    name: 'wpstudbook/layers/import',
    files: ['src/import/**/*.{ts,tsx}'],
    rules: {
      '@typescript-eslint/no-restricted-imports': [
        'error',
        {
          patterns: [layerImport('storage'), layerImport('services'), layerImport('ui'), REACT_PACKAGES, IDB_PACKAGE],
        },
      ],
    },
  },
  {
    name: 'wpstudbook/layers/storage',
    files: ['src/storage/**/*.{ts,tsx}'],
    rules: {
      '@typescript-eslint/no-restricted-imports': [
        'error',
        {
          patterns: [
            layerImport('domain', true),
            layerImport('import'),
            layerImport('services'),
            layerImport('ui'),
            REACT_PACKAGES,
          ],
        },
      ],
    },
  },
  {
    name: 'wpstudbook/layers/services',
    files: ['src/services/**/*.{ts,tsx}'],
    rules: {
      '@typescript-eslint/no-restricted-imports': ['error', { patterns: [layerImport('ui'), REACT_PACKAGES] }],
    },
  },
  {
    name: 'wpstudbook/layers/ui',
    files: ['src/ui/**/*.{ts,tsx}'],
    rules: {
      '@typescript-eslint/no-restricted-imports': [
        'error',
        {
          patterns: [layerImport('domain', true), layerImport('storage'), layerImport('import'), IDB_PACKAGE],
        },
      ],
    },
  },
];
```

- [ ] **Step 5：執行測試確認通過**

Run：`npx vitest run --project unit tests/unit/lint-layers.test.ts`
Expected：PASS，7 個測試通過。

- [ ] **Step 6：建立 ESLint 主設定與 Prettier 設定**

`eslint.config.js`：

```js
// @ts-check
import js from '@eslint/js';
import { defineConfig } from 'eslint/config';
import reactHooks from 'eslint-plugin-react-hooks';
import tseslint from 'typescript-eslint';
import { layerConfigs } from './eslint.layers.js';

export default defineConfig(
  {
    ignores: [
      'dist/**',
      'node_modules/**',
      'test-results/**',
      'playwright-report/**',
      'tests/fixtures/generated/**',
    ],
  },
  {
    files: ['**/*.{ts,tsx}'],
    extends: [js.configs.recommended, tseslint.configs.strictTypeChecked],
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
    rules: {
      '@typescript-eslint/no-floating-promises': 'error',
      '@typescript-eslint/consistent-type-imports': 'error',
    },
  },
  {
    files: ['src/ui/**/*.tsx', 'src/main.tsx'],
    plugins: { 'react-hooks': reactHooks },
    rules: {
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'error',
    },
  },
  {
    files: ['**/*.js'],
    extends: [js.configs.recommended],
    languageOptions: { sourceType: 'module' },
  },
  ...layerConfigs,
);
```

> 2026-09-15 偏離：實作省略了 `// @ts-check`。原因：eslint-plugin-react-hooks 7.1.1 的型別與 ESLint 10 的 `Plugin` 型別不相容，加上 `@ts-check` 會報錯；待上游修正後恢復。

`.prettierrc.json`：

```json
{
  "singleQuote": true,
  "printWidth": 100,
  "trailingComma": "all"
}
```

`.prettierignore`：

```gitignore
dist
node_modules
test-results
playwright-report
tests/fixtures/generated
package-lock.json
```

在 `package.json` 的 `scripts` 加入：

```json
"lint": "eslint . && prettier --check .",
"format": "prettier --write ."
```

- [ ] **Step 7：格式化並執行完整檢查**

Run：`npm run format`
Expected：Prettier 改寫格式不一致的檔案。

Run：`npm run lint`
Expected：ESLint 沒有錯誤，Prettier 回報所有檔案格式一致。若 ESLint 回報任務 1 的程式碼問題，依規則修正程式碼（不關閉規則），並重跑 `npm test` 確認測試仍通過。

Run：`npm test && npm run typecheck`
Expected：全部通過。

- [ ] **Step 8：Commit**

```bash
git add eslint.config.js eslint.layers.js .prettierrc.json .prettierignore package.json package-lock.json src scripts tests vite.config.ts vitest.config.ts WPStudBook.html tsconfig.json
git commit -m "加入 ESLint 分層規則與 Prettier" -m "分層引用規則集中在 eslint.layers.js，並以單元測試驗證違規會被擋下。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 3：環境檢查與 IndexedDB 探測

**Files:**
- Create: `src/storage/probe-counter.ts`、`src/services/environment-check.ts`、`src/ui/EnvironmentPanel.tsx`
- Modify: `src/ui/App.tsx`、`vitest.config.ts`
- Test: `tests/integration/probe-counter.test.ts`、`tests/unit/environment-check.test.ts`、`tests/integration/environment-check.test.ts`

**Interfaces:**
- Consumes：任務 1 的 `App`、Vitest 專案；任務 2 的分層規則。
- Produces：
  - `PROBE_DATABASE_NAME = 'wpstudbook-probe'`
  - `incrementProbeCounter(): Promise<number>`
  - `ENVIRONMENT_CHECK_KEYS = ['secureContext', 'randomUuid', 'sha256', 'compression', 'shiftJis', 'indexedDb'] as const`
  - `EnvironmentCheckKey = (typeof ENVIRONMENT_CHECK_KEYS)[number]`
  - `EnvironmentReport`：每個 `EnvironmentCheckKey` 一個 `boolean`；`failures: Partial<Record<EnvironmentCheckKey, string>>`（只含失敗項目的原因）；`probeCount?: number`（屬性皆為 `readonly`）
  - `runEnvironmentCheck(): Promise<EnvironmentReport>`
  - 畫面測試識別：`env-<key>`（內容「通過」或「失敗」）、`env-<key>-reason`（失敗原因，通過時為空字串）、`probe-count`（數字或「無法取得」）、按鈕「重新檢查」
  - 設計說明（使用者 2026-09-14 決定）：每項檢查以 try/catch 探測能力，失敗時回傳失敗並保留錯誤原因，不往外丟出例外

- [ ] **Step 1：安裝依賴並設定整合測試環境**

取得使用者同意後執行：

```bash
npm install --save-exact idb@8.0.3 react-aria-components@1.21.1
npm install --save-dev --save-exact fake-indexeddb@6.2.5
```

修改 `vitest.config.ts` 的 `integration` 專案，加入 `setupFiles`：

```ts
      {
        test: {
          name: 'integration',
          include: ['tests/integration/**/*.test.ts'],
          environment: 'node',
          setupFiles: ['fake-indexeddb/auto'],
        },
      },
```

- [ ] **Step 2：撰寫探測計數的失敗測試**

`tests/integration/probe-counter.test.ts`：

```ts
import { IDBFactory } from 'fake-indexeddb';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { incrementProbeCounter } from '../../src/storage/probe-counter.ts';

describe('incrementProbeCounter', () => {
  beforeEach(() => {
    vi.stubGlobal('indexedDB', new IDBFactory());
  });

  afterEach(() => {
    vi.unstubAllGlobals();
  });

  it('每次呼叫加 1，數值保存在 IndexedDB', async () => {
    expect(await incrementProbeCounter()).toBe(1);
    expect(await incrementProbeCounter()).toBe(2);
  });

  it('全新的資料庫從 1 開始', async () => {
    expect(await incrementProbeCounter()).toBe(1);
  });
});
```

Run：`npx vitest run --project integration tests/integration/probe-counter.test.ts`
Expected：FAIL，錯誤指出找不到 `src/storage/probe-counter.ts`。

- [ ] **Step 3：實作探測計數**

`src/storage/probe-counter.ts`：

```ts
import { openDB } from 'idb';

export const PROBE_DATABASE_NAME = 'wpstudbook-probe';

const STORE_NAME = 'counter';
const COUNTER_KEY = 'loadCount';

export async function incrementProbeCounter(): Promise<number> {
  const database = await openDB(PROBE_DATABASE_NAME, 1, {
    upgrade(upgradeDatabase) {
      upgradeDatabase.createObjectStore(STORE_NAME);
    },
  });
  try {
    const transaction = database.transaction(STORE_NAME, 'readwrite');
    const current: unknown = await transaction.store.get(COUNTER_KEY);
    const next = (typeof current === 'number' ? current : 0) + 1;
    await transaction.store.put(next, COUNTER_KEY);
    await transaction.done;
    return next;
  } finally {
    database.close();
  }
}
```

Run：`npx vitest run --project integration tests/integration/probe-counter.test.ts`
Expected：PASS，2 個測試通過。

- [ ] **Step 4：撰寫環境檢查的失敗測試**

`tests/unit/environment-check.test.ts`（Node 沒有 IndexedDB 與安全環境旗標）：

```ts
import { describe, expect, it } from 'vitest';
import { runEnvironmentCheck } from '../../src/services/environment-check.ts';

describe('runEnvironmentCheck（Node，無 IndexedDB）', () => {
  it('Node 24 具備 UUID、SHA-256、gzip 與 shift_jis 嚴格解碼', async () => {
    const report = await runEnvironmentCheck();
    expect(report).toMatchObject({ randomUuid: true, sha256: true, compression: true, shiftJis: true });
  });

  it('沒有安全環境旗標與 IndexedDB 時如實回報失敗與原因，且不帶探測次數', async () => {
    const report = await runEnvironmentCheck();
    expect(report.secureContext).toBe(false);
    expect(report.indexedDb).toBe(false);
    expect('probeCount' in report).toBe(false);
    expect(Object.keys(report.failures).sort()).toEqual(['indexedDb', 'secureContext']);
    expect(report.failures.secureContext).toBe('isSecureContext 不是 true');
    expect(report.failures.indexedDb).toContain('indexedDB');
  });
});
```

`tests/integration/environment-check.test.ts`（fake-indexeddb）：

```ts
import { IDBFactory } from 'fake-indexeddb';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { runEnvironmentCheck } from '../../src/services/environment-check.ts';

describe('runEnvironmentCheck（有 IndexedDB）', () => {
  beforeEach(() => {
    vi.stubGlobal('indexedDB', new IDBFactory());
  });

  afterEach(() => {
    vi.unstubAllGlobals();
  });

  it('IndexedDB 可用時回報通過並帶出探測次數，失敗清單只剩安全環境', async () => {
    const first = await runEnvironmentCheck();
    expect(first.indexedDb).toBe(true);
    expect(first.probeCount).toBe(1);
    expect(first.failures).toEqual({ secureContext: 'isSecureContext 不是 true' });
    const second = await runEnvironmentCheck();
    expect(second.probeCount).toBe(2);
  });
});
```

Run：`npm test`
Expected：FAIL，錯誤指出找不到 `src/services/environment-check.ts`。

- [ ] **Step 5：實作環境檢查**

`src/services/environment-check.ts`：

```ts
import { incrementProbeCounter } from '../storage/probe-counter.ts';

export const ENVIRONMENT_CHECK_KEYS = [
  'secureContext',
  'randomUuid',
  'sha256',
  'compression',
  'shiftJis',
  'indexedDb',
] as const;

export type EnvironmentCheckKey = (typeof ENVIRONMENT_CHECK_KEYS)[number];

export interface EnvironmentReport extends Readonly<Record<EnvironmentCheckKey, boolean>> {
  readonly failures: Readonly<Partial<Record<EnvironmentCheckKey, string>>>;
  readonly probeCount?: number;
}

type CheckResult = { readonly ok: true } | { readonly ok: false; readonly reason: string };

const PASSED: CheckResult = { ok: true };

const SHA256_INPUT = 'WPStudBook';
const SHA256_EXPECTED = '3438fdae4a98583178c51b7ee0e4d62813c2e016eca4f7e28e3f42fa621c1c11';
const CP932_SAMPLE = new Uint8Array([0x83, 0x65, 0x83, 0x58, 0x83, 0x67, 0x94, 0x6e]);
const CP932_SAMPLE_TEXT = 'テスト馬';
const CP932_TRUNCATED = new Uint8Array([0x83]);
const UUID_V4 = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;

function failed(reason: string): CheckResult {
  return { ok: false, reason };
}

function describeError(error: unknown): string {
  return error instanceof Error ? `${error.name}: ${error.message}` : String(error);
}

function toHex(buffer: ArrayBuffer): string {
  return Array.from(new Uint8Array(buffer), (byte) => byte.toString(16).padStart(2, '0')).join('');
}

function checkSecureContext(): CheckResult {
  return Reflect.get(globalThis, 'isSecureContext') === true
    ? PASSED
    : failed('isSecureContext 不是 true');
}

function checkRandomUuid(): CheckResult {
  try {
    const uuid = crypto.randomUUID();
    return UUID_V4.test(uuid) ? PASSED : failed(`產生的值不是 UUID v4：${uuid}`);
  } catch (error) {
    return failed(describeError(error));
  }
}

async function checkSha256(): Promise<CheckResult> {
  try {
    const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(SHA256_INPUT));
    const actual = toHex(digest);
    return actual === SHA256_EXPECTED ? PASSED : failed(`SHA-256 結果不符：${actual}`);
  } catch (error) {
    return failed(describeError(error));
  }
}

async function checkCompression(): Promise<CheckResult> {
  try {
    const text = SHA256_INPUT.repeat(20);
    const compressed = await new Response(
      new Blob([new TextEncoder().encode(text)]).stream().pipeThrough(new CompressionStream('gzip')),
    ).arrayBuffer();
    const header = new Uint8Array(compressed, 0, 2);
    if (header[0] !== 0x1f || header[1] !== 0x8b) {
      return failed('壓縮結果沒有 gzip 檔頭');
    }
    const restored = await new Response(
      new Blob([compressed]).stream().pipeThrough(new DecompressionStream('gzip')),
    ).text();
    return restored === text ? PASSED : failed('解壓後內容與原文不同');
  } catch (error) {
    return failed(describeError(error));
  }
}

function checkShiftJis(): CheckResult {
  try {
    const decoded = new TextDecoder('shift_jis', { fatal: true }).decode(CP932_SAMPLE);
    if (decoded !== CP932_SAMPLE_TEXT) {
      return failed(`CP932 解碼結果不符：${decoded}`);
    }
  } catch (error) {
    return failed(describeError(error));
  }
  try {
    new TextDecoder('shift_jis', { fatal: true }).decode(CP932_TRUNCATED);
    return failed('不完整的位元組沒有觸發嚴格解碼錯誤');
  } catch {
    // 預期結果：嚴格模式遇到不完整的位元組必須丟出錯誤。
    return PASSED;
  }
}

async function checkIndexedDb(): Promise<
  { readonly ok: true; readonly count: number } | { readonly ok: false; readonly reason: string }
> {
  try {
    return { ok: true, count: await incrementProbeCounter() };
  } catch (error) {
    return { ok: false, reason: describeError(error) };
  }
}

export async function runEnvironmentCheck(): Promise<EnvironmentReport> {
  const [sha256, compression, database] = await Promise.all([
    checkSha256(),
    checkCompression(),
    checkIndexedDb(),
  ]);
  const results: Readonly<Record<EnvironmentCheckKey, CheckResult>> = {
    secureContext: checkSecureContext(),
    randomUuid: checkRandomUuid(),
    sha256,
    compression,
    shiftJis: checkShiftJis(),
    indexedDb: database.ok ? PASSED : failed(database.reason),
  };

  const failures: Partial<Record<EnvironmentCheckKey, string>> = {};
  for (const key of ENVIRONMENT_CHECK_KEYS) {
    const result = results[key];
    if (!result.ok) {
      failures[key] = result.reason;
    }
  }

  const report = {
    secureContext: results.secureContext.ok,
    randomUuid: results.randomUuid.ok,
    sha256: results.sha256.ok,
    compression: results.compression.ok,
    shiftJis: results.shiftJis.ok,
    indexedDb: results.indexedDb.ok,
    failures,
  };
  return database.ok ? { ...report, probeCount: database.count } : report;
}
```

Run：`npm test`
Expected：PASS，`unit` 與 `integration` 專案全部通過。

- [ ] **Step 6：建立環境檢查面板**

`src/ui/EnvironmentPanel.tsx`：

```tsx
import { useCallback, useEffect, useState } from 'react';
import { Button } from 'react-aria-components';
import {
  runEnvironmentCheck,
  type EnvironmentCheckKey,
  type EnvironmentReport,
} from '../services/environment-check.ts';

const CHECK_LABELS = [
  ['secureContext', '安全環境（isSecureContext）'],
  ['randomUuid', 'crypto.randomUUID()'],
  ['sha256', 'SHA-256（crypto.subtle）'],
  ['compression', 'gzip（CompressionStream／DecompressionStream）'],
  ['shiftJis', 'CP932 嚴格解碼（TextDecoder shift_jis）'],
  ['indexedDb', 'IndexedDB 寫入與讀回'],
] as const satisfies ReadonlyArray<readonly [EnvironmentCheckKey, string]>;

export function EnvironmentPanel() {
  const [report, setReport] = useState<EnvironmentReport | undefined>(undefined);

  const check = useCallback(async () => {
    setReport(await runEnvironmentCheck());
  }, []);

  useEffect(() => {
    void check();
  }, [check]);

  return (
    <section aria-labelledby="environment-heading">
      <h2 id="environment-heading">環境檢查</h2>
      {report === undefined ? (
        <p role="status">檢查中…</p>
      ) : (
        <>
          <table>
            <tbody>
              {CHECK_LABELS.map(([key, label]) => (
                <tr key={key}>
                  <th scope="row">{label}</th>
                  <td data-testid={`env-${key}`}>{report[key] ? '通過' : '失敗'}</td>
                  <td data-testid={`env-${key}-reason`}>{report.failures[key] ?? ''}</td>
                </tr>
              ))}
            </tbody>
          </table>
          <p>
            IndexedDB 探測次數：
            <span data-testid="probe-count">
              {report.probeCount === undefined ? '無法取得' : String(report.probeCount)}
            </span>
          </p>
        </>
      )}
      <Button
        onPress={() => {
          void check();
        }}
      >
        重新檢查
      </Button>
    </section>
  );
}
```

修改 `src/ui/App.tsx`：

```tsx
import { EnvironmentPanel } from './EnvironmentPanel.tsx';

export function App() {
  return (
    <main>
      <h1>WPStudBook</h1>
      <p data-testid="app-version">版本 {__APP_VERSION__}</p>
      <EnvironmentPanel />
    </main>
  );
}
```

- [ ] **Step 7：型別、程式檢查與建置**

Run：`npm run typecheck && npm run lint && npm test && npm run build`
Expected：全部通過；建置最後一行輸出成品大小。把大小記在對話紀錄，任務 7 寫入設計決策。

- [ ] **Step 8：Commit**

```bash
git add package.json package-lock.json vitest.config.ts src tests
git commit -m "加入環境檢查面板與 IndexedDB 探測" -m "逐項檢查安全環境 API、gzip、CP932 嚴格解碼與 IndexedDB，結果顯示在畫面上供端到端測試讀取。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 4：Playwright 端到端測試（file://）

**Files:**
- Create: `playwright.config.ts`、`tests/e2e/helpers.ts`、`tests/e2e/environment.spec.ts`
- Modify: `package.json`（scripts）

**Interfaces:**
- Consumes：任務 3 的畫面測試識別與按鈕名稱；任務 1 的 `npm run build`。
- Produces：
  - 環境變數 `E2E_BROWSERS`（逗號分隔，可為 `msedge`、`chrome`；預設 `msedge`）
  - `APP_URL`（`tests/e2e/helpers.ts`，建置檔的 `file://` 網址）
  - npm 指令 `e2e`（不含 `@references` 標籤的測試）、`test:e2e`（先建置再執行 `e2e`）

- [ ] **Step 1：安裝 Playwright**

取得使用者同意後執行：

```bash
npm install --save-dev --save-exact @playwright/test@1.63.0
```

Expected：安裝完成。本計畫只使用已安裝的 Edge 與 Chrome，不執行 `npx playwright install`。

- [ ] **Step 2：建立 Playwright 設定與共用網址**

`playwright.config.ts`：

```ts
import { defineConfig, devices } from '@playwright/test';

type BrowserChannel = 'msedge' | 'chrome';

function selectedChannels(): BrowserChannel[] {
  const channels = (process.env.E2E_BROWSERS ?? 'msedge')
    .split(',')
    .map((item) => item.trim())
    .filter((item): item is BrowserChannel => item === 'msedge' || item === 'chrome');
  if (!channels.includes('chrome') && !process.env.CI) {
    console.warn('[e2e] 未執行 Chrome：E2E_BROWSERS 未包含 chrome，Chrome 由 GitHub Actions 驗證');
  }
  return channels;
}

export default defineConfig({
  testDir: 'tests/e2e',
  fullyParallel: false,
  forbidOnly: Boolean(process.env.CI),
  retries: 0,
  reporter: process.env.CI ? [['list'], ['html', { open: 'never' }]] : 'list',
  use: {
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: selectedChannels().map((channel) => ({
    name: channel,
    use: {
      ...(channel === 'msedge' ? devices['Desktop Edge'] : devices['Desktop Chrome']),
      channel,
    },
  })),
});
```

> 2026-09-15 偏離：實作將 `video` 設為 `'off'`，未使用計畫中的 `'retain-on-failure'`。原因：Playwright 的影片錄製需要另外下載 ffmpeg 執行檔，本計畫不下載額外執行檔；trace 與失敗自動截圖仍保留供除錯使用。

`tests/e2e/helpers.ts`：

```ts
import { resolve } from 'node:path';
import { pathToFileURL } from 'node:url';

export const APP_URL = pathToFileURL(resolve('dist/WPStudBook.html')).href;
```

在 `package.json` 的 `scripts` 加入：

```json
"e2e": "playwright test --grep-invert @references",
"test:e2e": "npm run build && npm run e2e"
```

- [ ] **Step 3：撰寫端到端測試**

`tests/e2e/environment.spec.ts`：

```ts
import { expect, test } from '@playwright/test';
import { APP_URL } from './helpers.ts';

const CHECK_KEYS = ['secureContext', 'randomUuid', 'sha256', 'compression', 'shiftJis', 'indexedDb'] as const;

test.describe('file:// 環境條件（設計決策 7.5 節）', () => {
  test('所有環境檢查都通過，且沒有發出任何網路請求', async ({ page }) => {
    const externalRequests: string[] = [];
    page.on('request', (request) => {
      const url = request.url();
      if (!url.startsWith('file:') && !url.startsWith('data:') && !url.startsWith('blob:')) {
        externalRequests.push(url);
      }
    });

    await page.goto(APP_URL);
    await expect(page.getByRole('heading', { name: 'WPStudBook' })).toBeVisible();
    for (const key of CHECK_KEYS) {
      await expect(page.getByTestId(`env-${key}-reason`), `環境檢查 ${key} 的失敗原因`).toHaveText('');
      await expect(page.getByTestId(`env-${key}`)).toHaveText('通過');
    }
    expect(externalRequests).toEqual([]);
  });

  test('重新整理後 IndexedDB 資料仍在', async ({ page }) => {
    await page.goto(APP_URL);
    const probeCount = page.getByTestId('probe-count');
    await expect(probeCount).toHaveText('1');
    await page.reload();
    await expect(probeCount).toHaveText('2');
  });

  test('React Aria 按鈕可用鍵盤操作，重新檢查會再寫入一次', async ({ page }) => {
    await page.goto(APP_URL);
    const probeCount = page.getByTestId('probe-count');
    await expect(probeCount).toHaveText('1');
    await page.getByRole('button', { name: '重新檢查' }).focus();
    await page.keyboard.press('Enter');
    await expect(probeCount).toHaveText('2');
  });
});
```

- [ ] **Step 4：執行端到端測試**

Run：`npm run test:e2e`
Expected：先輸出 `[e2e] 未執行 Chrome…` 警告，接著 `msedge` 專案 3 個測試通過。

任何環境檢查顯示「失敗」時，依設計決策 7.5 節停止後續任務：記錄是哪一項失敗、Edge 版本與錯誤畫面（`test-results/` 內的截圖），回報使用者並回到設計決策調整方案。

- [ ] **Step 5：程式檢查與 Commit**

Run：`npm run lint && npm run typecheck`
Expected：全部通過。

```bash
git add playwright.config.ts package.json package-lock.json tests/e2e
git commit -m "加入 file:// 端到端測試" -m "以 Playwright 驅動已安裝的 Edge 開啟建置檔，驗證環境檢查、無網路請求、重新整理後資料仍在與鍵盤操作。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 5：匯入格式表與本機實檔測試

**Files:**
- Create: `src/domain/import-type.ts`、`src/import/formats.ts`、`tests/references/samples.ts`、`tests/references/formats.test.ts`、`tests/e2e/references.spec.ts`
- Modify: `vitest.config.ts`、`package.json`（scripts）
- Test: `tests/unit/formats.test.ts`

**Interfaces:**
- Consumes：任務 4 的 `APP_URL`、`E2E_BROWSERS`。
- Produces：
  - `IMPORT_TYPES`、`ImportType`（`'jan2yo' | 'aprFoals' | 'mayMares' | 'julMares' | 'candidateFile' | 'mayStallions' | 'targetStallion' | 'octWorldMares'`）
  - `ImportFormatId = 'jan2yo' | 'aprFoals' | 'broodmare' | 'stallion'`
  - `HeaderExpectation = { position: number; name: string }`（位置從 1 起算）
  - `ImportFormat = { id: ImportFormatId; columnCount: number; headers: readonly HeaderExpectation[] }`
  - `IMPORT_FORMATS: Readonly<Record<ImportFormatId, ImportFormat>>`
  - `FORMAT_OF_IMPORT_TYPE: Record<ImportType, ImportFormatId>`
  - `findHeaderMismatches(format: ImportFormat, headerRow: readonly string[]): string[]`
  - `REFERENCE_SAMPLES`、`sampleExists(sample)`、`readSampleBytes(sample)`（`tests/references/samples.ts`）
  - Vitest 專案 `references`；npm 指令 `test:references`

- [ ] **Step 1：建立匯入類型列舉**

`src/domain/import-type.ts`：

```ts
export const IMPORT_TYPES = [
  'jan2yo',
  'aprFoals',
  'mayMares',
  'julMares',
  'candidateFile',
  'mayStallions',
  'targetStallion',
  'octWorldMares',
] as const;

export type ImportType = (typeof IMPORT_TYPES)[number];
```

- [ ] **Step 2：撰寫格式表的失敗測試**

`tests/unit/formats.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { IMPORT_TYPES } from '../../src/domain/import-type.ts';
import {
  FORMAT_OF_IMPORT_TYPE,
  IMPORT_FORMATS,
  findHeaderMismatches,
  type ImportFormat,
} from '../../src/import/formats.ts';

function headerRowFor(format: ImportFormat): string[] {
  const row = Array.from({ length: format.columnCount }, (_, index) => `欄${String(index + 1)}`);
  for (const { position, name } of format.headers) {
    row[position - 1] = name;
  }
  return row;
}

describe('匯入格式表（附錄 A）', () => {
  it('每種匯入類型都對應到一種格式', () => {
    for (const importType of IMPORT_TYPES) {
      expect(IMPORT_FORMATS[FORMAT_OF_IMPORT_TYPE[importType]]).toBeDefined();
    }
  });

  it('表頭位置都在欄數範圍內且不重複，最後一欄是空白', () => {
    for (const format of Object.values(IMPORT_FORMATS)) {
      const positions = format.headers.map((header) => header.position);
      expect(new Set(positions).size).toBe(positions.length);
      expect(positions.every((position) => position >= 1 && position <= format.columnCount)).toBe(true);
      expect(format.headers).toContainEqual({ position: format.columnCount, name: '' });
    }
  });

  it('符合格式的表頭沒有差異', () => {
    for (const format of Object.values(IMPORT_FORMATS)) {
      expect(findHeaderMismatches(format, headerRowFor(format))).toEqual([]);
    }
  });

  it('欄位名稱不同時指出位置與預期名稱', () => {
    const format = IMPORT_FORMATS.broodmare;
    const row = headerRowFor(format);
    row[56] = '別的欄';
    expect(findHeaderMismatches(format, row)).toEqual(['第 57 欄應為「能力番号」，實際為「別的欄」']);
  });

  it('欄數不同時回報欄數與缺少的欄', () => {
    const format = IMPORT_FORMATS.stallion;
    const row = headerRowFor(format).slice(0, -1);
    expect(findHeaderMismatches(format, row)).toEqual([
      '欄數應為 63，實際為 62',
      '第 63 欄應為「」，實際為「（不存在）」',
    ]);
  });
});
```

Run：`npx vitest run --project unit tests/unit/formats.test.ts`
Expected：FAIL，錯誤指出找不到 `src/import/formats.ts`。

- [ ] **Step 3：實作格式表**

`src/import/formats.ts`（表頭名稱與位置取自需求規格附錄 A，已與 `.references/` 實檔逐欄核對）：

```ts
import type { ImportType } from '../domain/import-type.ts';

export type ImportFormatId = 'jan2yo' | 'aprFoals' | 'broodmare' | 'stallion';

export interface HeaderExpectation {
  readonly position: number;
  readonly name: string;
}

export interface ImportFormat {
  readonly id: ImportFormatId;
  readonly columnCount: number;
  readonly headers: readonly HeaderExpectation[];
}

function headers(entries: Readonly<Record<number, string>>): HeaderExpectation[] {
  return Object.entries(entries).map(([position, name]) => ({ position: Number(position), name }));
}

export const IMPORT_FORMATS: Readonly<Record<ImportFormatId, ImportFormat>> = {
  jan2yo: {
    id: 'jan2yo',
    columnCount: 78,
    headers: headers({
      1: '馬名', 2: '国', 3: '年', 4: '性', 5: 'SP', 6: 'ST',
      8: '力', 9: '瞬', 10: '勝', 11: '柔', 12: '精', 13: '賢', 14: '健', 15: 'サ',
      17: '芝', 18: 'ダ', 19: '距離適性', 32: '父系', 35: '子出',
      51: '父馬', 52: '母馬', 53: '牝系', 62: '生産国', 63: '生牧', 64: '馬主', 65: '繋牧',
      71: '史実', 73: '史実番号', 74: '能力番号', 75: '馬番号', 77: '馬名', 78: '',
    }),
  },
  aprFoals: {
    id: 'aprFoals',
    columnCount: 63,
    headers: headers({
      1: '馬名', 3: '年', 4: '性', 5: 'SP', 6: 'ST',
      8: '力', 9: '瞬', 10: '勝', 11: '柔', 12: '精', 13: '賢', 14: '健', 15: 'サ',
      17: '芝', 18: 'ダ', 27: '距離適性', 28: '子出',
      46: '父馬', 47: '父系', 48: '母馬', 49: '牝系', 51: '生牧', 52: '馬主', 53: '繋牧',
      57: '史実番号', 58: '能力番号', 59: '馬番号', 61: '馬名', 63: '',
    }),
  },
  broodmare: {
    id: 'broodmare',
    columnCount: 61,
    headers: headers({
      1: '馬名', 3: '年', 4: 'SP', 5: 'ST',
      6: '力', 7: '瞬', 8: '勝', 9: '柔', 10: '精', 11: '賢', 12: '健', 13: 'サ',
      16: '子出', 18: '芝', 19: 'ダ', 23: '距離適性', 27: '活力', 37: '繁年', 38: '繁頭',
      43: '父馬', 44: '父系', 45: '母馬', 46: '牝系', 48: '牧場', 49: '状態', 50: '種付け種牡馬',
      52: '特殊配合', 56: '史実番号', 57: '能力番号', 58: '馬番号', 59: '馬名', 61: '',
    }),
  },
  stallion: {
    id: 'stallion',
    columnCount: 63,
    headers: headers({
      1: '馬名', 2: '国', 3: '年', 4: 'SP', 5: 'ST',
      6: '力', 7: '瞬', 8: '勝', 9: '柔', 10: '精', 11: '賢', 12: '健', 13: 'サ',
      16: '子出', 18: '芝', 19: 'ダ', 23: '距離適性', 33: '父系', 37: '種付料',
      49: '父馬', 50: '母馬', 51: '牝系', 52: '生産国', 53: '牧場', 54: '特殊配合',
      56: '現役', 57: '史実', 58: '史実番号', 59: '能力番号', 60: '馬番号', 62: '馬名', 63: '',
    }),
  },
};

export const FORMAT_OF_IMPORT_TYPE = {
  jan2yo: 'jan2yo',
  aprFoals: 'aprFoals',
  mayMares: 'broodmare',
  julMares: 'broodmare',
  candidateFile: 'broodmare',
  octWorldMares: 'broodmare',
  mayStallions: 'stallion',
  targetStallion: 'stallion',
} as const satisfies Record<ImportType, ImportFormatId>;

export function findHeaderMismatches(format: ImportFormat, headerRow: readonly string[]): string[] {
  const problems: string[] = [];
  if (headerRow.length !== format.columnCount) {
    problems.push(`欄數應為 ${String(format.columnCount)}，實際為 ${String(headerRow.length)}`);
  }
  for (const { position, name } of format.headers) {
    const actual = headerRow[position - 1];
    if (actual !== name) {
      problems.push(`第 ${String(position)} 欄應為「${name}」，實際為「${actual ?? '（不存在）'}」`);
    }
  }
  return problems;
}
```

執行 `npm run format` 後，Prettier 會把 `headers({ ... })` 內每個鍵值排成一行，屬正常結果。

Run：`npx vitest run --project unit tests/unit/formats.test.ts`
Expected：PASS，5 個測試通過。

- [ ] **Step 4：建立本機實檔測試**

`tests/references/samples.ts`（只記錄檔名與對應格式，不含檔案內容）：

```ts
import { existsSync, readFileSync } from 'node:fs';
import { join } from 'node:path';
import type { ImportFormatId } from '../../src/import/formats.ts';

export interface ReferenceSample {
  readonly fileName: string;
  readonly formatId: ImportFormatId;
}

const REFERENCE_DIR = '../.references';

export const REFERENCE_SAMPLES: readonly ReferenceSample[] = [
  { fileName: '1968年 1月1週._二歲新馬.txt', formatId: 'jan2yo' },
  { fileName: '1968年 4月1週_幼駒誕生.txt', formatId: 'aprFoals' },
  { fileName: '1968年 5月1週_繁殖牝馬.txt', formatId: 'broodmare' },
  { fileName: '1968年 7月1週_繁殖牝馬.txt', formatId: 'broodmare' },
  { fileName: '1968年10月1週_繁殖牝馬.txt', formatId: 'broodmare' },
  { fileName: '1968年5月1週_種牡馬.txt', formatId: 'stallion' },
];

function samplePath(sample: ReferenceSample): string {
  return join(REFERENCE_DIR, sample.fileName);
}

export function sampleExists(sample: ReferenceSample): boolean {
  return existsSync(samplePath(sample));
}

export function readSampleBytes(sample: ReferenceSample): Uint8Array {
  return new Uint8Array(readFileSync(samplePath(sample)));
}
```

`tests/references/formats.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { IMPORT_FORMATS, findHeaderMismatches } from '../../src/import/formats.ts';
import { REFERENCE_SAMPLES, readSampleBytes, sampleExists } from './samples.ts';

describe('本機實檔：CP932 解碼、表頭與每列欄數（設計決策 7.4 節）', () => {
  for (const sample of REFERENCE_SAMPLES) {
    it.skipIf(!sampleExists(sample))(`${sample.fileName}（檔案不存在時略過，視為未執行）`, () => {
      const format = IMPORT_FORMATS[sample.formatId];
      const text = new TextDecoder('shift_jis', { fatal: true }).decode(readSampleBytes(sample));
      const lines = text.split('\r\n').filter((line) => line !== '');
      expect(findHeaderMismatches(format, (lines[0] ?? '').split('\t'))).toEqual([]);

      const badLineNumbers = lines
        .map((line, index) => ({ lineNumber: index + 1, columns: line.split('\t').length }))
        .filter(({ columns }) => columns !== format.columnCount)
        .map(({ lineNumber }) => lineNumber);
      expect(badLineNumbers).toEqual([]);
    });
  }
});
```

`tests/e2e/references.spec.ts`（比對 Edge 與 Node 的解碼結果，只輸出是否一致）：

```ts
import { expect, test } from '@playwright/test';
import { REFERENCE_SAMPLES, readSampleBytes, sampleExists } from '../references/samples.ts';
import { APP_URL } from './helpers.ts';

test.describe('@references 本機實檔：瀏覽器與 Node 的 CP932 解碼結果一致', () => {
  for (const sample of REFERENCE_SAMPLES) {
    test(sample.fileName, async ({ page }) => {
      test.skip(!sampleExists(sample), '檔案不存在，未執行');
      const bytes = Array.from(readSampleBytes(sample));
      const nodeText = new TextDecoder('shift_jis', { fatal: true }).decode(new Uint8Array(bytes));

      await page.goto(APP_URL);
      const browserText = await page.evaluate(
        (input) => new TextDecoder('shift_jis', { fatal: true }).decode(new Uint8Array(input)),
        bytes,
      );
      expect(browserText === nodeText, '瀏覽器與 Node 解碼結果不同').toBe(true);
    });
  }
});
```

在 `vitest.config.ts` 的 `projects` 陣列末尾加入：

```ts
      {
        test: {
          name: 'references',
          include: ['tests/references/**/*.test.ts'],
          environment: 'node',
        },
      },
```

在 `package.json` 的 `scripts` 加入：

```json
"test:references": "vitest run --project references && npm run build && playwright test --grep @references"
```

- [ ] **Step 5：執行本機實檔測試**

先確認實檔仍存在（`.references/` 內容可能隨時變動）：

Run：`ls ../.references/*.txt`
Expected：列出本任務 `REFERENCE_SAMPLES` 內的 6 個檔案；缺少的檔案會在測試中略過並標示未執行。

Run：`npm run test:references`
Expected：Vitest `references` 專案 6 個測試通過；Playwright `msedge` 專案 6 個 `@references` 測試通過。輸出只有檔名與通過與否，沒有檔案內容。

若 Node 或 Edge 解碼失敗、或兩者結果不同，停止後續任務並回報：這代表 `TextDecoder('shift_jis')` 與 CP932 有差異，需回到設計決策調整解碼方案。

Run：`npm test && npm run e2e && npm run lint && npm run typecheck`
Expected：全部通過；`npm run e2e` 不執行 `@references` 測試。

- [ ] **Step 6：Commit**

```bash
git add src/domain src/import tests/unit/formats.test.ts tests/references tests/e2e/references.spec.ts vitest.config.ts package.json
git commit -m "加入匯入格式表與本機實檔測試" -m "依附錄 A 建立各格式欄數與關鍵表頭位置表；本機實檔測試驗證 CP932 解碼、表頭與每列欄數，並比對 Edge 與 Node 解碼一致。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 6：情境覆蓋清單與一鍵檢查

**Files:**
- Create: `scripts/lib/scenario-coverage.ts`、`scripts/scenarios.ts`
- Modify: `package.json`（scripts）
- Test: `tests/unit/scenario-coverage.test.ts`

**Interfaces:**
- Consumes：repo 根目錄 `docs/需求規格.md`（從 `app/` 讀取為 `../docs/需求規格.md`）第 15 章的情境格式 `- **ID-01**：…`；測試標題標籤格式 `[ID-01]`。
- Produces：
  - `extractScenarioIds(specMarkdown: string): string[]`
  - `extractTaggedIds(testSource: string): Set<string>`
  - `findUncoveredScenarios(scenarioIds: readonly string[], taggedIds: ReadonlySet<string>): string[]`
  - npm 指令 `scenarios`、`check`

- [ ] **Step 1：撰寫失敗測試**

`tests/unit/scenario-coverage.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import {
  extractScenarioIds,
  extractTaggedIds,
  findUncoveredScenarios,
} from '../../scripts/lib/scenario-coverage.ts';

const SPEC = [
  '## 14. 成品與技術限制',
  '- **BLD-99**：第 15 章以外的代號不算',
  '## 15. 驗收情境',
  '### 馬匹身分（ID）',
  '- **ID-01**：條件 → 結果',
  '- **ID-02**：條件 → 結果',
  '### 八系管理（LINE）',
  '- **LINE-14**：條件 → 結果',
  '## 16. 待確認事項',
  '- **ID-99**：第 15 章以外的代號不算',
].join('\n');

describe('情境覆蓋', () => {
  it('只取第 15 章的情境代號', () => {
    expect(extractScenarioIds(SPEC)).toEqual(['ID-01', 'ID-02', 'LINE-14']);
  });

  it('從測試原始碼取出方括號標籤', () => {
    const source = "it('[ID-01] 同一匹馬', () => {});\ntest('[LINE-14] 配對距離', async () => {});\n";
    expect([...extractTaggedIds(source)]).toEqual(['ID-01', 'LINE-14']);
  });

  it('列出沒有任何測試標籤的情境', () => {
    expect(findUncoveredScenarios(['ID-01', 'ID-02', 'LINE-14'], new Set(['ID-01']))).toEqual([
      'ID-02',
      'LINE-14',
    ]);
  });
});
```

Run：`npx vitest run --project unit tests/unit/scenario-coverage.test.ts`
Expected：FAIL，錯誤指出找不到 `scripts/lib/scenario-coverage.ts`。

- [ ] **Step 2：實作情境覆蓋函式**

`scripts/lib/scenario-coverage.ts`：

```ts
const SCENARIO_ID = /\*\*([A-Z]+-\d{2})\*\*/g;
const TEST_TAG = /\[([A-Z]+-\d{2})\]/g;

export function extractScenarioIds(specMarkdown: string): string[] {
  const afterChapter15 = specMarkdown.split(/^## 15\. /m)[1] ?? '';
  const chapter15 = afterChapter15.split(/^## 16\. /m)[0] ?? '';
  const ids = Array.from(chapter15.matchAll(SCENARIO_ID), (match) => match[1] ?? '');
  return [...new Set(ids.filter((id) => id !== ''))];
}

export function extractTaggedIds(testSource: string): Set<string> {
  const ids = Array.from(testSource.matchAll(TEST_TAG), (match) => match[1] ?? '');
  return new Set(ids.filter((id) => id !== ''));
}

export function findUncoveredScenarios(
  scenarioIds: readonly string[],
  taggedIds: ReadonlySet<string>,
): string[] {
  return scenarioIds.filter((id) => !taggedIds.has(id));
}
```

Run：`npx vitest run --project unit tests/unit/scenario-coverage.test.ts`
Expected：PASS，3 個測試通過。

- [ ] **Step 3：建立輸出腳本與一鍵檢查指令**

`scripts/scenarios.ts`：

```ts
import { readdirSync, readFileSync } from 'node:fs';
import { join } from 'node:path';
import {
  extractScenarioIds,
  extractTaggedIds,
  findUncoveredScenarios,
} from './lib/scenario-coverage.ts';

function listTestFiles(directory: string): string[] {
  return readdirSync(directory, { recursive: true, withFileTypes: true })
    .filter((entry) => entry.isFile() && /\.(test|spec)\.tsx?$/.test(entry.name))
    .map((entry) => join(entry.parentPath, entry.name));
}

const scenarioIds = extractScenarioIds(readFileSync('../docs/需求規格.md', 'utf8'));
const taggedIds = new Set<string>();
for (const file of listTestFiles('tests')) {
  for (const id of extractTaggedIds(readFileSync(file, 'utf8'))) {
    taggedIds.add(id);
  }
}

const uncovered = findUncoveredScenarios(scenarioIds, taggedIds);
const covered = scenarioIds.length - uncovered.length;
console.log(
  `情境總數 ${String(scenarioIds.length)}，已有測試 ${String(covered)}，尚無測試 ${String(uncovered.length)}`,
);
if (uncovered.length > 0) {
  console.log(uncovered.join('、'));
}
```

在 `package.json` 的 `scripts` 加入：

```json
"scenarios": "node scripts/scenarios.ts",
"check": "npm run lint && npm run typecheck && npm test && npm run test:e2e"
```

- [ ] **Step 4：執行並確認結果**

Run：`npm run scenarios`
Expected：第一行為 `情境總數 211，已有測試 0，尚無測試 211`（階段 0 沒有情境測試），第二行列出 211 個代號。

Run：`npm run check`
Expected：lint、typecheck、單元與整合測試、建置、Edge 端到端測試全部通過。

- [ ] **Step 5：Commit**

```bash
git add scripts tests/unit/scenario-coverage.test.ts package.json
git commit -m "加入情境覆蓋清單與一鍵檢查指令" -m "npm run scenarios 列出規格第 15 章尚無測試的情境；npm run check 依序執行全部檢查。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### 任務 7：GitHub Actions 與階段 0 收尾

**Files:**
- Create（repo 根目錄）: `.github/workflows/ci.yml`、`.github/workflows/release.yml`
- Modify: `package.json`（版本）、`package-lock.json`、repo 根目錄的 `docs/設計決策.md`（7.5 節結果）與 `docs/開發計畫.md`（目前進度）

**Interfaces:**
- Consumes：npm 指令 `lint`、`typecheck`、`test`、`build`、`e2e`；環境變數 `E2E_BROWSERS`；建置檔 `dist/WPStudBook.html`。
- Produces：CI 工作 `檢查與建置`、`端到端（msedge）`、`端到端（chrome）`；建置檔 artifact 名稱 `WPStudBook-html`；發布流程（標籤 `v*` 觸發，本計畫不觸發）。

- [ ] **Step 1：建立 CI 工作流程**

`.github/workflows/ci.yml`：

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

defaults:
  run:
    working-directory: app

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: 檢查與建置
    runs-on: ubuntu-latest
    outputs:
      code_changed: ${{ steps.changes.outputs.code_changed }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json
      - name: 判斷是否只改文件
        id: changes
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
          else
            base="${{ github.event.before }}"
          fi
          if [ -z "$base" ] || [ "$base" = "0000000000000000000000000000000000000000" ]; then
            echo "code_changed=true" >> "$GITHUB_OUTPUT"
          elif git diff --name-only "$base" "${{ github.sha }}" | grep -Ev '^docs/|\.md$' > /dev/null; then
            echo "code_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "code_changed=false" >> "$GITHUB_OUTPUT"
          fi
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - uses: actions/upload-artifact@v7
        with:
          name: WPStudBook-html
          path: app/dist/WPStudBook.html
          if-no-files-found: error

  e2e:
    name: 端到端（${{ matrix.browser }}）
    needs: check
    if: needs.check.outputs.code_changed == 'true'
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [msedge, chrome]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - uses: actions/download-artifact@v8
        with:
          name: WPStudBook-html
          path: app/dist
      - name: 執行端到端測試
        run: npm run e2e
        env:
          E2E_BROWSERS: ${{ matrix.browser }}
      - name: 保存失敗報告
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report-${{ matrix.browser }}
          path: |
            app/playwright-report
            app/test-results
```

- [ ] **Step 2：建立發布工作流程**

`.github/workflows/release.yml`：

```yaml
name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

defaults:
  run:
    working-directory: app

jobs:
  check:
    name: 檢查與建置
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - name: 確認標籤與 package.json 版本一致
        shell: bash
        run: |
          expected="v$(node -p "require('./package.json').version")"
          if [ "$expected" != "$GITHUB_REF_NAME" ]; then
            echo "標籤 $GITHUB_REF_NAME 與 package.json 版本 $expected 不一致" >&2
            exit 1
          fi
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - uses: actions/upload-artifact@v7
        with:
          name: WPStudBook-html
          path: app/dist/WPStudBook.html
          if-no-files-found: error

  e2e:
    name: 端到端（${{ matrix.browser }}）
    needs: check
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [msedge, chrome]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - uses: actions/download-artifact@v8
        with:
          name: WPStudBook-html
          path: app/dist
      - run: npm run e2e
        env:
          E2E_BROWSERS: ${{ matrix.browser }}

  publish:
    name: 建立 GitHub Release
    needs: e2e
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: WPStudBook-html
          path: app/dist
      - name: 發布並附上 WPStudBook.html
        env:
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "$GITHUB_REF_NAME" dist/WPStudBook.html
          --repo "$GITHUB_REPOSITORY"
          --title "WPStudBook $GITHUB_REF_NAME"
          --generate-notes
          --verify-tag
```

- [ ] **Step 3：本機完整檢查並 Commit**

Run：`npm run format && npm run check`
Expected：全部通過（Prettier 也會格式化兩個 YAML 檔）。

```bash
git add ../.github package.json
git commit -m "加入 GitHub Actions CI 與發布流程" -m "CI 在 Ubuntu 檢查與建置，Windows 以 Edge 與 Chrome 執行端到端測試；推送 v* 標籤時發布 Release。" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 4：推送並開 draft PR 觸發 CI**

CI 只在 PR 與推送 `main` 時執行，所以要開 PR 才能在分支上驗證 Chrome。這是對外動作，先在對話中取得使用者同意，再依序執行 `/repo-push` 與 `/repo-open-pr`（draft）。

Run：`gh pr checks --watch`
Expected：`檢查與建置`、`端到端（msedge）`、`端到端（chrome）` 三個工作都成功。

任一工作失敗時，下載 `playwright-report-*` artifact 查看原因：
- Windows 環境沒有 Chrome 或 Edge：回報使用者，回到設計決策 D-05 調整方案。
- 環境檢查某項在 Chrome 失敗：比照任務 4 Step 4 停止並回報。
- 其他錯誤：修正後提交並推送到同一支分支，重新確認。

- [ ] **Step 5：記錄驗證結果並升版**

在 `docs/設計決策.md` 的 7.5 節清單之後加入以下內容，數值與網址換成本次實際結果：

```markdown
**驗證結果（階段 0）**

| 項目 | 結果 |
| --- | --- |
| 驗證日期 | 實際日期 |
| 本機 Edge | 實際版本，任務 4 與任務 5 的端到端測試通過 |
| GitHub Actions | 實際 CI 執行網址；Edge、Chrome 端到端測試通過 |
| 內嵌 module script、安全環境 API、gzip、CP932 解碼、IndexedDB 重新整理 | 全部通過 |
| Edge 與 Node 解碼六份實檔 | 結果一致 |
| 成品大小 | `npm run build` 輸出的實際 KB 數 |
```

在 `docs/開發計畫.md` 文件資訊的「目前進度」改為：

```markdown
- 目前進度：階段 0 已完成（設計決策 D-01～D-12 確認、工具鏈與 CI 建立、`file://` 環境條件在 Edge 與 Chrome 驗證通過，結果見設計決策 7.5 節）。下一步為階段 1：資料安全核心。
```

依設計決策 8.1 節，完成一個計畫階段升 x：

Run：`npm version 0.1.0 --no-git-tag-version`
Expected：`package.json` 與 `package-lock.json` 的版本改為 `0.1.0`，沒有建立 git 標籤。

Run：`npm run check`
Expected：全部通過。

- [ ] **Step 6：Commit 並推送**

```bash
git add ../docs/設計決策.md ../docs/開發計畫.md package.json package-lock.json
git commit -m "記錄階段 0 驗證結果並升版為 0.1.0" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

取得使用者同意後以 `/repo-push` 推送到同一支分支，並以 `gh pr checks --watch` 確認 CI 仍全部通過。PR 是否合併、是否發布 `v0.1.0` Release，由使用者決定。
