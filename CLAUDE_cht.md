> 本文件是 `CLAUDE.md` 的繁體中文參考版，不是 Agent 指引入口。`CLAUDE.md` 為正式規則，兩者衝突時以 `CLAUDE.md` 為準並回報不一致。只在雙語維護或不一致檢查時讀取本文件，修改時兩個檔案須在同一次變更中同步更新。
>
> 本文件不含 `CLAUDE.md` 開頭的 `@AGENTS.md` 匯入行：那一行是 Claude Code 實際載入規則的機制，只能放在正式規則檔，複製到參考版會造成重複載入。

## Claude Code

- `AGENTS.md` 是正式規則來源。本檔案只放 Claude Code 專用的補充；兩者衝突時以 `AGENTS.md` 為準並回報。
- Claude Code / Cowork 的暫存內容放在 `.claude-tmp/YYYY-MM-DD_<task-name>/`。絕不可碰 `.codex-tmp/`。

## 作業方式

以下是使用者對本專案 agent 作業方式的常設指示，除非使用者在該次對話另行更改，否則每次對話都適用。這套流程取代子計畫 2-2 之前那套「每個任務都走 brief、implementer、reviewer、多輪修正」的繁重做法——那套的成本遠高於它帶來的價值。

規則放在這裡是因為它描述的是 Claude Code 怎麼做事，不是產品本身是什麼。若要讓 Codex / Work 也照同一套跑，就搬到 `AGENTS.md`（並在同一次變更同步 `AGENTS_cht.md`）。

### 範圍

- 產品範圍不縮減。需求規格、剩餘各階段與 211 項驗收情境都要完成，MVP 不是完成線。
- 只能靠真實遊戲匯出檔確認的情境，在合成資料驗證通過後於 `docs/驗收追蹤.md` 標為 `待真實樣本`。

### 批次

- 不寫大型實作計畫，也不在動手前貼出完整的預定程式碼。`docs/實作計畫/` 只保留階段 0 到 2 寫過的計畫，新的子計畫不再產生計畫文件。
- 3 到 5 個相關任務合成一批。該批由主流程自己實作、測試與檢查。子代理只用於真正可平行的獨立工作，最多同時兩個，且絕不做 reviewer 審 reviewer。
- 每批結束給簡短狀態（完成內容、跑了哪些測試、commit、下一批）後繼續，不在批次之間停下來等同意。

### 測試

- npm 指令一律在 `app/` 內執行。
- 開發過程中只跑與該次變更相關的測試。
- 每批結束跑 `npm run format`、`npm run lint`、`npm run typecheck`，以及單元與整合測試。只有動到介面的批次才跑相關的端到端測試。
- 完整套件（`npm run check`）在子計畫結束時跑一次，建立 Pull Request 前再跑一次。

### 審查

- 整體審查在子計畫結束時做一次，不是每個任務做一次。
- Critical 與 Important 的發現在子計畫完成前處理。Minor 集中記錄，在子計畫最後一個 commit 前處理。

### 文件

- 只在行為改變、驗收狀態改變，或發現規格自相矛盾時更新 `docs/`。每批的進度摘要放在 agent 暫存區，不放 `docs/`。
- `docs/驗收追蹤.md` 的證據欄先填測試檔，CI 執行網址放在 Pull Request 說明，CI 證據行則在下一支分支的第一個 commit 補進 `docs/驗收追蹤.md`。不要安排「CI 通過後再推同一個 PR」的步驟：使用者一看到 CI 綠燈就會合併，那個 commit 一定來不及。

### 何時停下來問

- 只有以下三種情況停下來問：會實質改變產品行為的規格衝突、缺少真實遊戲資料（只有使用者才知道的遊戲行為），以及破壞性操作之前。
- commit 後直接推送，不必先問。開 Pull Request、合併、刪除分支一律先問。
- 其他所有設計取捨，直接採用你會建議的選項，並寫進 Pull Request 說明或 `docs/設計決策.md` 供事後審閱。不要停下來問。

### 視覺設計

- 依使用者 2026-09-19 的裁定，視覺設計是階段 5 的子計畫 5-3：排在效能（5-2）之後，讓虛擬化先定下清單與卡片結構；排在可及性（5-4）之前，讓對比、焦點與強制色彩只需對最終樣式檢查一次。
- 5-3 開始前維持目前的基本樣式，不主動加入視覺設計任務，回報畫面時要明說目前仍是基本樣式。
